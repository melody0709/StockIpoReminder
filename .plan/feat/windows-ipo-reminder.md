# Windows A 股新股申购提醒：0.3.1 当前实现与后续收口、简化计划

- 文档状态：已有功能可靠性闭环基线；P0 公告链路减重已完成；OCR 明确不做；其他未开发扩展暂停
- 当前应用版本：0.3.1，以 Cargo.toml 为唯一版本事实来源
- 当前技术栈：Rust 2024、Slint、windows-rs、reqwest、rusqlite、WiX MSI
- 本次复核日期：2026-08-26
- 当前验证平台：Windows 11 Pro Build 26200 x64
- 最低技术目标：Windows Build 19041、x64
- 产品边界：只读取公开发行信息、在本地生成提醒并记录人工确认；不登录券商、不读取账户、不自动下单

本文档用于准确记录当前源码、生成物和验证证据，并区分四类后续事项：已经闭环、已实现但仍需实机或外部条件验收、为减轻程序而已经退役的既有能力、尚未实现且暂停或明确不做的扩展。当前基线已经完成既有功能的可靠性收口和 P0 产品减重；OCR 已确认不符合简单提醒工具的边界，不再作为研究或开发候选。多设备同步、采集服务拆分、服务端推送、邮件、短信和通用 Webhook 等未开发能力继续保留状态记录，但不属于当前必须开发项。状态冲突时按以下顺序判断：

1. Cargo.toml 与当前源码。
2. 当前版本生成物和本轮验证报告。
3. README.md 与 RELEASE_NOTES.md。
4. 本文档中的历史说明。

旧 C#、WPF、.NET 测试和旧安装器证据不再证明当前 Rust 版本。

## 0. 当前结论

Stock IPO Reminder 已完成从 C#、WPF 到 Rust、Slint 的正式迁移。当前仓库的正式运行载荷是单个 StockIpoReminder.exe，发布形式为 x64 MSI 和便携 ZIP，不依赖 .NET Runtime。

0.3.1 已具备可运行的核心业务闭环：

- 四来源候选采集、多来源裁决，以及只保存标题、来源、发布时间和官方 URL 的正式公告元数据检索。
- SQLite 持久化、逐只确认与撤销、关键变化后重新确认。
- 四态同步结论、来源计数审计、来源健康和持久化退避。
- 06:00–22:00 自动同步窗口、前夜和 08:00 定点核验、错过补做与确定性抖动。
- 持久化提醒 Outbox、跨重启 lease 恢复，以及窗口可见、未最小化并与显示器工作区相交后才完成投递。
- 独立置顶提醒窗、同轮聚合、托盘常驻、第二次启动唤醒已有实例，以及 Explorer 重建后重新注册图标。
- 原生 WinRT Windows Toast、稳定 AUMID、MSI 开始菜单快捷方式绑定，以及 Toast 失败后的托盘气泡回退。
- 专用提醒窗、Windows Toast、托盘气泡回退、声音和任务栏闪烁的分通道自检。
- 用户明确确认已申购后，按已知公告日期安排中签查询和缴款资金提醒；撤销确认会取消这些提醒并恢复申购提醒。
- 用户明确确认已申购后，按已知上市日期安排上市日 08:30 本地提醒；不读取持仓、不跟踪价格或收益。
- 今日任务与未来 60 天支持文本、市场和状态组合筛选，并使用 Slint ListView 虚拟化任务卡片。
- 设置页可从当前用户进程启动安全 MSI 卸载；默认保留数据，可选清理必须精确确认且只允许当前用户默认数据目录。
- 每日备份、日志保留、运维健康、诊断 ZIP 和异常退出报告。
- Windows Time 服务检查、同 EXE Watchdog、崩溃循环限制、数据库迁移前备份。
- 主窗口和托盘先呈现，数据库备份、迁移、完整性检查与必要压缩在后台完成；就绪前数据库操作有统一门禁。
- 设置页提供当前本地缓存/数据目录入口；提醒通道卡片为完整测试矩阵预留自适应高度，不再截断底部测试项。
- x64 MSI、便携 ZIP、发布布局、Windows smoke、发布审计和联网内存测量。
- 可选 Authenticode/RFC 3161 签名构建、detached CMS 更新清单、签名证书固定、MSI 安装版安全自动更新和短生命周期更新 helper。
- 用户明确同意后可发送二次脱敏的受控 Watchdog 崩溃报告；默认关闭，接收地址和隐私政策必须同时配置为无凭据 HTTPS。
- 可选企业微信、钉钉、飞书或 PushPlus 第二通知通道；默认关闭，凭据用 Windows DPAPI 当前用户范围加密，并由独立持久化 Outbox、租约恢复、重试和小时配额控制发送。

### 0.1 产品减重决策（2026-08-26）

- 新股提醒的核心字段以 Eastmoney、SSE、CNINFO、BSE 的结构化数据为准，包括代码、简称、申购代码、申购日期、发行价格、申购上限、市值/资金要求，以及上游公布后的中签、缴款和上市日期。字段尚未公布时保持“待公布”，由后续同步补齐，不通过 PDF 或 OCR 猜测。
- OCR 永久排除出产品范围。扫描件、加密文件和异常版式不做图像识别或版面理解；用户需要核对时直接打开官方公告链接。
- 不再研究 PDF 按页/按对象解析、外部低内存解析器或 OCR 引擎。
- P0 已停止自动下载和解析公告 PDF，并移除 `lopdf`、PDF Worker、对应 Job Object 与解析状态链路；当前只保留公告标题、来源、发布时间和官方 URL，允许用户用浏览器手动核对原文。
- 当前版本不再产生新的 `AnnouncementVerified` 状态或本地公告文件；旧数据库中的历史状态、公告记录和已有 PDF 继续兼容读取，但不会自动删除。
- 退役 PDF 自动处理不得削弱四来源采集、数量审计、来源健康、冲突提示、缺失字段保留和“来源不完整时不得宣称今日无新股”等现有可靠性边界。

仍须避免以下误判：

- Windows Toast 主路径已经实现，但安装版真实呈现、通知点击激活、通知中心留存及各种系统通知设置仍缺实机证据；便携版没有 MSI 快捷方式注册时允许明确降级到托盘气泡。
- 专用提醒窗已实现，但真实提醒触发下的不抢焦点、多屏和混合 DPI 证据仍需补。
- Watchdog 是同 EXE 的用户态监督进程，不是 Windows Service；单次恢复、崩溃循环上限和监督进程被结束后的主进程行为已有矩阵证据，但同时结束监督进程和主程序后仍无法继续恢复，注销和关机行为仍待实机验证。
- MSI 不直接修改用户数据库。当前保护是在新版本第一次打开旧数据库、执行 schema 迁移之前创建完整性校验备份；它不是 MSI deferred custom action。
- Windows 10、RDP、多屏混合缩放和完整 MSI 安装/升级/回滚/修复/卸载矩阵仍缺实机证据；Explorer 自动重启恢复已由用户确认闭环；安全卸载已有源码、单元测试和 authoring 门禁，但尚未在已安装产品上执行破坏性 smoke。
- Authenticode、签名更新清单和自动更新主路径已经实现，但当前 0.3.1 基线未接入正式 CA 证书和 HTTPS 稳定版源，发布清单仍明确为 `signed=false`，应用内更新保持关闭；正式证书、真实 RFC 3161 时间戳、SmartScreen 信誉和线上更新验收仍待完成。
- 崩溃报告客户端主路径已经实现，但当前 0.3.1 发布未配置接收服务和隐私政策，release manifest 中两个地址均为 null，上传保持关闭；正式接收端、服务端校验、保留/删除政策和隐私文本仍待外部接入。
- 第二通知通道是用户配置的本地客户端补充投递，不是服务端推送或多设备同步；邮件、短信、任意自建 Webhook、企业代理和自定义 CA 仍未实现。
- 旧 PDF Worker 的资源边界和内存报告仅作为历史基线保留；当前源码已移除 Worker 和 `lopdf`，同步不再创建 PDF 子进程、下载临时文件或新增公告 PDF。

## 1. 0.3.1 可复现发布基线

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| Cargo 版本 | 0.3.1 | Cargo.toml |
| Rust/Slint 测试 | 84/84 通过 | `rtk cargo test` 已通过；删除 4 项 PDF 下载/解析专用测试后，package 阶段仍执行 `rtk cargo test --locked` |
| 233 MiB 旧库启动迁移 | 界面与托盘 17 ms 呈现；数据库 233,574,400 → 782,336 bytes | 2026-08-26 隔离数据根实测；schema v8→v9，正文 0，空闲页 0 |
| Release 构建 | 通过 | rtk cmd /c build.bat --package |
| 构建布局 | 通过 | scripts/validate-build-layout.ps1，runtime 文件数 4 |
| Windows smoke | schema v10，18/18 检查通过 | build/artifacts/tests/smoke/windows-rust-0.3.1-20260827-000741.json |
| 签名/更新集成 | 6/6 检查通过；本次 P0 未改签名/更新路径，未重复执行 | build/artifacts/tests/signing-update/signing-update-0.3.1-20260826-222236.json |
| Release audit | schema v2，10/10 通过；本次 P0 未改审计规则，未重复执行 | build/artifacts/tests/audit/release-rust-0.3.1-20260826-222630.json |
| 联网同步/内存 | schema v2，通过；同步完成，意外子进程和退役 PDF 临时文件均为 0 | build/artifacts/diagnostics/memory/rust-0.3.1-20260827-000819.json |
| Watchdog 恢复矩阵 | 通过；本次 P0 未改监督协议，未重复执行 | build/artifacts/tests/watchdog/watchdog-matrix-0.3.1-20260826-222244.json |
| 可运行 EXE | 已生成 | build/run/x64-release/StockIpoReminder.exe |
| 便携 ZIP | 已生成 | build/packages/0.3.1/StockIpoReminder-0.3.1-win-x64-portable.zip |
| MSI | 已生成 | build/packages/0.3.1/StockIpoReminder-0.3.1-win-x64.msi |
| 发布清单 | 已生成；`signed=false`，崩溃接收端/隐私政策均未配置 | build/packages/0.3.1/release-manifest.json |
| 校验和 | 已生成 | build/packages/0.3.1/SHA256SUMS.txt |

注：本轮 smoke 和联网内存报告的文件名由脚本按本机本地日期生成，因此包含 `20260827`；报告 JSON 内的 `generatedAtUtc` 分别为 `2026-08-26T16:08:10Z` 和 `2026-08-26T16:08:39Z`。

当前发布物：

| 文件 | 大小 | SHA-256 |
| --- | ---: | --- |
| StockIpoReminder-0.3.1-win-x64-portable.zip | 7,673,095 bytes | 9a7b8a694801157f532d929711f92e254901b213be76829a74fce347876e4e08 |
| StockIpoReminder-0.3.1-win-x64.msi | 6,340,608 bytes | ccffc7a2283e99aa59a9ba5c570fb70cd60110c4eed5c39e564beab6783d5bfe |

Windows smoke 空闲采样：

- Private Bytes：5,660,672 bytes。
- Working Set：33,980,416 bytes。
- 便携自检、W32Time 与 Windows Toast 诊断、后台 UI、专用提醒窗不抢焦点、第二次启动唤醒、模拟托盘图标丢失后的 TaskbarCreated 重新注册、恢复消息防抖、MSI 管理安装映像、MSI 载荷自检、可选安装目录、AUMID、安全卸载、签名更新、崩溃报告隐私和第二通知通道安全 authoring 均通过。

联网同步内存报告：

- 主进程峰值：18,657,280 bytes Private Bytes / 46,706,688 bytes Working Set，低于 100 MiB 门禁。
- 进程族峰值与主进程相同；意外子进程峰值数量 0，意外子进程内存峰值 0。
- 同步后主进程：7,352,320 / 10,199,040 bytes。
- 结束后残留意外子进程 0，退役 PDF 临时文件 0；隔离数据目录没有新增 PDF 文件。

Watchdog 恢复矩阵：

- 单次强杀后按 2 秒退避启动替代主进程，监督进程最终以退出码 0 正常结束。
- 10 分钟窗口内连续强杀 4 次生成 4 份异常报告；第 4 次不再安排重启，监督进程以非零退出码停止，崩溃循环上限生效。
- 强制结束监督进程后，已启动的主进程继续运行，并按测试定时器正常退出。

构建警告：

- test 目标有 8 个、release 目标有 11 个 dead_code 或未读取字段警告。
- WiX 构建为 0 warning、0 error。
- 当前警告不影响运行或发布，但后续清理无用 API 时应避免误删为兼容、诊断或未来功能保留的字段。

## 2. 状态标记

- **已实现**：源码存在完整主路径，并有自动化测试、发布脚本或当前版本运行证据。
- **已实现，证据待补**：源码主路径完整，但特定 Windows 环境或真实交互场景尚未形成版本化报告。
- **已实现，外部接入待补**：源码、构建和离线验证主路径完整，但正式凭据、线上服务或发布环境尚未接入，不能等同于正式上线验收。
- **部分实现**：已有基础能力，但语义、交互、可靠性或验证范围仍低于目标。
- **未实现**：当前源码没有对应主路径，保留为后续研究项。
- **历史替代**：旧方案被新的技术决策替代，不应照搬旧代码，但原产品目的继续保留。

## 3. 功能状态总表

| 领域 | 当前状态 | 当前实现 | 后续状态 |
| --- | --- | --- | --- |
| Rust/Slint 正式应用 | 已实现 | 根目录 Cargo 项目，单 Rust EXE | 继续保持 Rust-only 发布门禁 |
| 托盘常驻与单实例 | 已实现 | 原生托盘、数据目录级命名 Mutex、关闭主窗后继续运行；第二次启动会广播数据目录专属消息并唤醒已有主窗口 | 补安装版、RDP 和启动竞态下的交互实机证据 |
| Explorer 托盘恢复 | 已实现，已确认 | 监听 TaskbarCreated 并重新执行 Shell_NotifyIcon NIM_ADD；schema v10 smoke 会先 NIM_DELETE 模拟图标丢失，再发送真实注册消息并验证重新注册成功；用户已确认真实 Explorer 重启后的恢复可用 | 已闭环，不再列为后续待办 |
| 登录自启动 | 已实现/历史替代 | HKCU Run，按数据根目录生成独立值名，清理旧计划任务 | 企业策略、漫游配置和被安全软件阻止时的诊断 |
| 异常退出 Watchdog | 已实现 | 同 EXE 监督进程；2/10/30 秒退避；10 分钟最多重启 3 次；JSON 报告；单次恢复、四次崩溃上限和监督进程退出矩阵已通过 | 补 Windows 注销、关机及监督进程与主程序同时终止的边界说明和实机证据 |
| 今日任务/未来 60 天 | 已实现 | Slint 分页，托盘菜单可直达；支持简称/代码、市场、状态组合筛选，任务卡片使用 ListView 虚拟化并带无障碍列表项标签；2,000 条固定任务的组合筛选回归已通过 | 仍需真实滚动性能、键盘导航和无障碍实测 |
| 逐只确认与撤销 | 已实现 | 仅申购日当天可确认；截止前可撤销；存储层再次校验 | 补更多时间边界和跨版本实机 smoke |
| 关键变化后重确认 | 已实现 | 代码、日期、价格、上限、单位、市值/资金、发行状态、官方时段和资金规则变化会递增事件版本 | 连续多次变化和复核中再次变化的专项报告 |
| 非关键变化通知 | 已实现 | 简称、历史代码、中签/缴款/上市日期、公告链接变化发送一次 DataChanged | 后续可增加变化前后对照 UI |
| 提醒计划 | 已实现 | 未确认阶段覆盖前一日 20:00、08:30、开盘前、小时、11:20、12:55、15/5/2 分钟和最终提醒；确认后按已知日期增加中签日 18:00、缴款日 08:30/14:00、上市日 08:30；显式当前时间、过期日期跳过、旧任务不重建历史提醒的确定性矩阵已通过 | 仍需节假日、时区和真实长时间运行证据 |
| 专用提醒窗 | 已实现，证据待补 | 独立 Window、置顶、WS_EX_NOACTIVATE、WS_EX_TOOLWINDOW、右下角定位、稍后处理和二次确认入口 | 真实提醒下验证焦点、多屏、任务栏位置和混合 DPI |
| 同轮提醒聚合 | 已实现 | 单股多个到期提醒合并；多股批量显示但不提供全部确认 | 逐只卡片式批量窗口和可访问性 |
| 持久化 Outbox | 已实现 | Pending/Leased/Delivered/Cancelled/Failed、租约过期重领、版本去重；到期判断包含等于当前时间，同股过期低等级提醒折叠到最新等级 | 仍需更长时间强杀和真实磁盘故障注入 |
| 实际显示后完成 | 已实现 | 延迟检查原生窗口确实可见、未最小化且与显示器工作区相交后才完成 lease；失败按 1/5/15/30 分钟退避并进入健康摘要，持续 15 分钟升级为 Failed | 仍需真实渲染失败、显示器热插拔和多屏移动证据 |
| 系统恢复 | 已实现，实机证据待补 | 休眠恢复、解锁、时间变化、网络恢复触发检查和同步；schema v10 smoke 注入 power/unlock/time 消息并验证 5 秒防抖；历史过期提醒折叠回归已通过 | 补真实睡眠、RDP 和长时间恢复风暴测试 |
| Windows Toast/气泡/声音/闪烁 | 已实现，证据待补 | 稳定 AUMID 的 WinRT Toast；失败后回退带 NIIF_RESPECT_QUIET_TIME 的 Shell_NotifyIcon 气泡；MessageBeep、FlashWindowEx | 安装版真实呈现、点击激活、通知中心、专注助手和勿扰模式矩阵 |
| 通知通道自检 | 已实现 | 专用提醒窗、Windows Toast、托盘气泡回退、声音、任务栏闪烁分开测试；Toast 或气泡回退任一通过可满足已启用的系统通知通道 | 增加系统策略、通知被禁用和无交互环境报告 |
| 四来源采集 | 已实现 | Eastmoney、SSE、CNINFO、BSE | 持续监控在线契约变化 |
| Collector 数量审计 | 已实现 | 审计声明数、明细数和身份校验数，不一致时来源 Warning | 增加真实上游分页、截断和重复记录样本 |
| 正式公告 | 已简化 | SSE、CNINFO、BSE，沪市可用巨潮镜像兜底；只保存标题、来源、发布时间和官方 URL，不请求 PDF 正文 | 持续监控元数据检索契约和链接有效性 |
| PDF Worker | 已移除 | `lopdf`、Worker 命令入口、请求/响应临时文件、PDF 下载和 Windows Job Object 限制均已从当前源码移除 | 旧实现证据仅作历史记录，不重新引入自动正文解析 |
| 数据质量与同步结论 | 已实现 | Unknown、HealthyNonempty、HealthyEmpty、DegradedCached；必要来源完整才允许健康空结果 | 完整来源故障矩阵和更多在线证据 |
| 来源退避 | 已实现 | Retry-After 优先；1/2/4/8/15/30 分钟指数退避；持久化抖动 | 继续验证极端 Retry-After 和系统时间跳变 |
| 退避期健康探测 | 已实现 | 10 分钟后开始，最长每 30 分钟一次，只探测可达性，不提前解除 API 退避 | 代理、DNS、TLS 和离线网络矩阵 |
| 自动同步调度 | 已实现 | 06:00–22:00；前夜 20:00；申购日 08:00；错过补做；普通日 0–90 秒、申购日 0–20 秒抖动；跨日窗口和时钟回拨输入已有确定性回归 | 仍需节假日和真实长时间运行报告 |
| 每日健康摘要 | 已实现 | 08:00 精确边界，数据库唯一键跨重启去重 | 无任务、部分来源失败、全部失败的 UI 截图和实机报告 |
| 系统时间检查 | 已实现 | Microsoft/Cloudflare HTTPS Date 样本，加 W32Time 服务状态；自动化矩阵覆盖无样本、单样本、正常偏差、超过五分钟偏差、服务停止和状态读取失败 | 仍需真实服务禁用、权限限制和代理篡改环境证据 |
| SQLite 数据层 | 已实现 | WAL、migration v1–v9、事件/来源/公告/确认、本地与第二通知 Outbox、发送尝试和健康表；同步审计只存元数据与哈希，不保存接口正文 | 继续保持每次 schema 演进独立 migration |
| 启动与数据库就绪 | 已实现 | UI/托盘先呈现；备份、迁移、完整性检查和必要压缩在后台执行；就绪前设置、任务、确认、人工覆盖与诊断入口统一拒绝数据库访问 | 继续观察极慢磁盘和安全软件环境，不再把后台准备误记为首屏阻塞 |
| 设置页本地缓存入口 | 已实现 | 显示当前数据根目录并可直接打开；默认指向 `%LocalAppData%\StockIpoReminder`，入口不执行删除或清理 | 后续如增加清理功能，必须独立确认并保留安全边界 |
| 每日维护与备份 | 已实现 | 每小时独立触发；每日 SQLite 在线备份；每 1,024 页仅让出 1 ms；唯一临时文件、完整性检查、`sync_all`、原子提交；提交前中断会保留既有备份并清理临时文件；保留 7 份 | 仍需磁盘满、只读目录和真实强杀进程故障注入 |
| 版本迁移前备份 | 已实现 | 根据实际 schema 版本判断，只在需要迁移时于 initialize 前创建并校验备份；失败则不迁移；application-version 标记继续使用 Windows 写穿原子替换 | 仍需真实旧版本→0.3.1 MSI 与便携升级矩阵 |
| 日志与诊断 | 已实现 | 按日期日志、单日分段、14 天保留；诊断 ZIP schema v4；近期同步、健康、本地与第二通知 Outbox、提醒、崩溃及 Windows Toast 诊断；不包含第二通道凭据 | 一键复制摘要、用户可选时间范围 |
| MSI/便携包 | 已实现/历史替代 | x64 per-machine MSI、可选目录、Major Upgrade、Portable ZIP；开始菜单快捷方式写入稳定 AUMID | 真实安装、升级、回滚、修复、卸载及安装版 Toast 矩阵 |
| 卸载删除数据选项 | 已实现，实机证据待补 | 设置页检测本产品 MSI 后提供入口；默认保留数据；可选清理要求精确短语，通过 UpgradeCode 查询 ProductCode，临时 helper 等主进程退出后调用 msiexec；仅 MSI 成功后删除当前用户默认 LocalAppData 数据目录 | 在隔离 Windows 虚拟机补普通卸载保留数据、确认清理、取消/UAC 拒绝、MSI 失败和重启码矩阵 |
| DPI/响应式布局 | 已实现，证据待补 | Per-Monitor V2、760×460 最小客户区、紧凑布局 | Windows 10、RDP、多屏混合缩放实机验证 |
| Authenticode 签名 | 已实现，外部接入待补 | `build.bat --package --sign` 支持当前用户证书或 PFX；校验 Code Signing EKU，以 SHA-256/RFC 3161 签名并复核 EXE/MSI；PFX 通过 .NET X509Store 短期导入并清理；含受保护 CI 工作流和临时证书集成测试 | 接入正式 CA 证书，在隔离签名机/受保护 CI 完成真实时间戳、签名审计、SmartScreen 和杀毒软件验收；当前基线仍为 unsigned |
| 自动更新 | 已实现，外部接入待补 | 设置页支持启动检查、手动检查和明确下载安装；只对已注册 MSI 开放；HTTPS + detached CMS + 证书 SHA-256 固定 + MSI 大小/哈希/Authenticode；helper 等主进程退出后调用 msiexec；未编译正式源和指纹时明确关闭 | 部署原子发布的 HTTPS stable 源，使用正式签名包验证在线升级、断网/篡改拒绝、失败回滚、旧版保留和灰度策略 |
| 崩溃上传 | 已实现，外部接入待补 | 默认关闭；只有接收端和隐私政策同时为无凭据 HTTPS 时可用；用户可手动发送或选择异常退出后自动发送；仅处理 `diagnostics/crashes` 顶层 128 KiB 内的 Watchdog JSON，递归删除敏感字段并再次脱敏；禁止重定向，30 秒超时，滚动 24 小时最多 3 次尝试，成功 SHA-256 去重 | 部署正式接收端和隐私政策，落实服务端认证/校验、限流、审计、保留与删除治理；当前发布 manifest 两个地址均为 null |
| 第二通知通道 | 已实现 | 默认关闭；支持企业微信、钉钉、飞书、PushPlus；只接受服务商官方 HTTPS 地址，PushPlus 使用固定端点；DPAPI 当前用户凭据、独立持久化 Outbox、2 分钟租约、最多 5 次发送、1/5/15/30 分钟退避、20 批次/小时、30/90 天清理，并与桌面提醒成败解耦；凭据轮换和写穿原子替换回归已通过 | 必须补服务商真实账号端到端实测；远端撤回、邮件、短信、通用 Webhook、企业代理、自定义 CA 和服务端推送按未开发范围暂停 |
| 中签/缴款提醒 | 已实现 | 仅在用户明确确认已申购后，按已知中签结果日和缴款日安排本地查询/资金提示；不读取账户、不推断中签、不判断缴款完成；过去日期、当前日期、缺失日期、撤销和待复核矩阵已通过 | 仍需更多真实公告覆盖和真实提醒交互证据 |
| 上市日提醒 | 已实现 | 仅在用户明确确认已申购、公开数据给出上市日且设置开启时，于上市日 08:30 本地提醒；不读取持仓、不跟踪价格或收益；过去日期和待复核矩阵已通过 | 仍需更多真实上市公告覆盖和真实交互证据 |
| OCR | 明确不做 | 结构化来源已满足提醒核心字段；扫描、加密或异常版式公告由用户通过官方链接人工核验 | 永久排除出路线图，不引入 OCR 引擎或版面理解依赖 |
| 多设备同步 | 未实现，暂停 | 当前完全本地单机 | 保留研究项，当前不开发 |
| 采集服务拆分 | 未实现，暂停 | 采集与桌面提醒同属本地应用 | 保留研究项，当前不开发 |
| 服务端推送 | 未实现，暂停 | 当前由桌面客户端直连用户配置的服务商，不存在中心消息服务、账号体系或设备注册 | 保留研究项，当前不开发 |

## 4. 当前架构

~~~text
Watchdog 监督进程
        │ 异常退出后受限重启
        ▼
Slint 主窗口 ◄──── Runtime 线程 ───── Durable Reminder Outbox
     │                 │                         │
     ▼                 │ 同步/恢复/维护          ├──► 专用提醒窗 ──► 严格可见性确认
原生托盘               ▼                         ├──► WinRT Toast ──失败──► 托盘气泡
     │        SQLite / 健康 / 备份 / 诊断         └──► Secondary Notification Outbox
     │                 ▲                                      │
     └── 声音/闪烁     │ 申购及申购后 Reminder Planner        ├──► 企业微信/钉钉/飞书官方 HTTPS
                       │                                      └──► PushPlus 固定 HTTPS 端点
       Eastmoney / SSE / CNINFO / BSE                         ▲
                        │                                      │
        公告元数据检索 → 标题/时间/官方 URL              DPAPI 当前用户凭据

HTTPS stable 更新源 → CMS 清单/证书固定 → MSI 下载验证 → 临时更新 helper

Watchdog JSON → 用户同意 → 二次脱敏/大小边界/限流/去重 → HTTPS 接收端（当前未配置）
~~~

### 4.1 模块职责

| 路径 | 当前职责 |
| --- | --- |
| src/main.rs | 进程入口、第二次启动唤醒、Slint 回调、提醒聚合、本地分通道自检、第二通知配置/测试 UI、更新 UI、托盘线程、恢复事件和 UI 刷新 |
| src/watchdog.rs | 主程序监督、受限重启、崩溃循环保护和本地异常报告 |
| src/updater.rs | HTTPS 签名清单、CMS/证书固定、版本与 Windows Build 约束、MSI 流式下载、Authenticode 复核和更新 helper |
| src/crash_upload.rs | 可选崩溃报告双 HTTPS 配置、受控文件选择、递归脱敏、无重定向上传、滚动限流、SHA-256 去重及本地结果状态 |
| src/secondary_notification.rs | 企业微信/钉钉/飞书/PushPlus 官方端点校验、Windows DPAPI 凭据保存、消息脱敏、HTTPS 请求和服务商响应校验 |
| src/model.rs | 领域枚举、事件、设置、第二通知服务商、公告、来源、同步结论和健康模型 |
| src/core.rs | 市场识别、申购及申购后提醒规划、多源裁决、事件哈希和变化原因 |
| src/network.rs | 四来源请求、JSON/JSONP 解析、白名单、数量审计和健康探测 |
| src/announcement.rs | SSE、CNINFO、BSE 公告元数据检索、相关性过滤、去重和官方 URL 记录 |
| src/runtime.rs | 自动同步调度、公告处理、本地与第二通知投递循环、健康摘要、时间检查和维护 |
| src/storage.rs | SQLite schema、迁移、确认、本地与第二通知 Outbox、发送配额/重试历史、健康、备份和维护 |
| src/operations.rs | 日志、脱敏、诊断 ZIP schema v4、自测试和版本迁移前备份 |
| src/windows_integration.rs | 单实例与唤醒、窗口样式、WinRT Toast、AUMID 与通知状态诊断、气泡回退、W32Time、声音、闪烁、目录和 HKCU Run |
| src/deployment.rs | 安全 MSI 卸载入口、UpgradeCode→ProductCode 查询、当前用户临时 helper、确认短语和数据根目录约束；同时保留历史内置部署兼容路径 |
| ui/main.slint | 主界面、详情、设置、申购/覆盖/卸载确认对话框、安全自动更新、可选崩溃报告、第二通知通道和专用提醒窗 |
| packaging/windows | WiX x64 per-machine MSI、安装目录 UI、稳定升级标识，以及带稳定 AUMID 的开始菜单快捷方式 |
| scripts/build-release.ps1 | 未签名/签名发布、PFX 或当前用户证书选择、RFC 3161、CMS 更新清单、可选崩溃接收/隐私地址、发布清单和校验和 |

### 4.2 进程与线程

- 正常启动先运行轻量 Watchdog 监督进程，再启动同 EXE 的主程序子进程。
- Watchdog 持有独立监督 Mutex；主程序持有数据目录级应用 Mutex。
- 同一数据目录再次启动时不会创建第二套 UI；它会向已有托盘消息窗口广播数据目录专属激活消息，并在主窗口实际可见后记录确认事件。
- 主程序使用 Slint 事件循环；隐藏最后一个窗口不会退出。
- Runtime 在线程 stock-ipo-runtime 中处理同步、投递、健康和维护。
- 第二通知自动批次由 Runtime 线程从独立 Outbox 租赁并发送；用户主动“发送测试”在短生命周期后台线程执行，二者在联网前共享小时配额预留。
- 托盘在线程 stock-ipo-native-tray 中处理菜单、气泡和 Windows 广播。
- 更新检查和下载在后台线程执行，不阻塞 Slint 事件循环。
- 用户明确安装后复制同 EXE 为短生命周期更新 helper；helper 等待主程序退出，再调用 Windows Installer。
- 自测试、部署 helper、更新 helper 和显式 no-watchdog 模式不会再创建监督层；同步链路不再创建 PDF Worker 子进程。

## 5. 关键业务不变量

### 5.1 确认语义

- 每只股票独立确认，禁止一键确认同日全部股票。
- 只有任务所列申购日当天可以确认。
- 显示窗口、发送气泡、关闭窗口、稍后处理、锁屏、休眠或程序重启都不等于已申购。
- 确认只代表用户在本程序中的人工记录，不代表券商已受理。
- 只有用户明确确认已申购后，才允许创建中签查询和缴款资金提醒；程序不得根据时间或页面状态自动推断已申购。
- 撤销确认必须事务性取消中签/缴款提醒，并恢复仍适用的申购提醒。
- 关键字段变化后旧确认必须失效并进入待复核。
- 撤销确认、生命周期恢复和提醒重排必须处于同一事务。

### 5.2 数据可靠性

- 任何单一来源失败都不能清空其他来源已经发现的任务。
- 只有启用市场的必要来源完整成功，且数量审计一致，才允许显示“今日无新股”。
- 上游缺失可选字段不得自动清空已有可信值。
- 公告元数据的检索地址和保存的原文 URL 必须继续遵守官方域名白名单；程序不得主动请求 URL 指向的 PDF 正文。
- 扫描、加密、超限或异常版式公告不得触发 OCR；只保留官方链接供用户自行打开。
- 不得重新引入 PDF Worker、自动 PDF 下载或新的公告正文解析器。
- 版本迁移前备份失败时不得继续 schema 迁移。
- 已提交的版本标记、DPAPI 凭据和崩溃上传状态不得通过“先删旧文件再重命名”更新；替换必须请求 Windows 写穿语义。

### 5.3 提醒可靠性

- Outbox 是提醒事实来源，UI channel 只是投递通道。
- 旧事件版本的 Pending、Leased 和 Failed 提醒必须取消。
- 只有持有有效 lease 的投递才能完成或失败。
- 专用提醒窗只有在原生窗口可见、未最小化且与显示器工作区相交后，才可把对应 lease 标记为完成。
- 单股多个到期提醒可合并显示，但不得合并确认语义。
- 程序恢复时不得重新创建整天已经过期的申购提醒；同股多个过期等级只保留最新到期等级。
- 投递失败必须采用有界退避并进入健康摘要，不能形成紧密失败循环或静默丢失。
- 强杀、租约过期和数据库重开后不得丢失尚未完成的提醒。

### 5.4 产品边界

- 不保存券商账号、密码、验证码、持仓、市值、资金或交易凭据。
- 不读取可申购额度。
- 不调用券商下单接口。
- 不验证委托是否成功。
- 不读取或推断是否中签，也不判断是否已经完成缴款。
- 中签日 18:00、缴款日 08:30/14:00 只是本地提示点，不代表交易所、主承销商或券商截止时间。
- 不做收益预测。
- 不绕过登录、验证码、WAF 或访问控制。
- 不把第三方开源项目、AKShare 或 Tushare 作为唯一生产来源。

### 5.5 安全卸载与数据删除

- 普通卸载默认保留数据库、设置、公告缓存、日志和备份。
- 应用内卸载只接受由稳定 UpgradeCode `A007780A-97A8-483E-A532-F336649EC5BB` 枚举到的当前已安装 ProductCode，不允许传入任意 MSI 标识。
- 临时 helper 以发起操作的当前用户身份运行，等待主程序完全退出后再调用 `msiexec /x ... /passive /norestart`。
- 只有用户勾选数据清理并准确输入 `删除当前用户数据` 时，才允许进入清理分支；Rust helper 会再次独立校验短语。
- 清理目标必须精确等于当前用户 `%LocalAppData%\StockIpoReminder`，拒绝自定义目录、其他用户目录和包含 `..` 的路径，也不枚举用户配置文件。
- 只有 Windows Installer 返回成功或“成功但需要重启”后才删除数据；取消、UAC 拒绝、超时或其他失败必须完整保留数据。
- 便携版没有 MSI 注册时不启用应用内卸载入口，用户退出后自行删除便携文件。

### 5.6 签名发布与自动更新

- 未同时编译 HTTPS stable 清单 URL 和合法 64 位证书 SHA-256 指纹时，应用内更新必须保持关闭。
- 便携版和未注册的 MSI 载荷不得启用应用内安装；不得把便携版静默转换为安装版。
- detached CMS 清单签名和 MSI Authenticode 必须固定到同一证书 SHA-256；产品、stable 通道、严格 `x.y.z` 版本、最低 Windows Build、大小和哈希必须全部通过。
- 只允许升级到高于当前 `CARGO_PKG_VERSION` 的版本，不接受降级、同版本、预发布或非三段数字版本。
- 下载的 MSI 只允许位于当前数据根目录的 `temp\updates`，安装 helper 必须再次复核路径、SHA-256、系统信任和签名证书指纹。
- 自动更新只能自动检查；下载和安装必须由用户明确点击。任何下载、CMS、哈希、证书、Windows Installer 或退出等待失败都不得启动或继续安装。
- 临时自签名测试根的放宽只存在于 `--update-bundle-self-test` 诊断命令；生产检查、下载和安装路径始终要求 Windows 系统信任。

### 5.7 第二通知通道安全与投递边界

- 第二通知通道默认关闭，只是本地桌面提醒的补充；远程发送失败不得改变或阻断桌面提醒的完成、确认和撤销语义。
- 企业微信、钉钉和飞书只接受对应官方主机与固定路径的无凭据 HTTPS Webhook；PushPlus 只接受 token，并固定发送到官方 HTTPS 端点。
- Webhook 和 token 必须用 Windows DPAPI 当前用户范围加密；不得进入 SQLite、日志、诊断 ZIP、数据库备份、命令行、环境变量或发布清单。
- 自动批次和用户主动测试必须在联网前统一预留小时配额；租约、失败退避、最多尝试次数和历史清理都必须持久化且有界。
- 关闭通道或切换服务商时，旧服务商尚未完成的队列必须取消，不得把旧消息投递到新目标。
- 当前边界不包含邮件、短信、任意自建 Webhook、企业代理、自定义 CA、客户端证书、端到端加密、服务端推送或多设备同步。

## 6. 当前实现细节

### 6.1 同步与来源健康

自动同步调度包含：

- 06:00 之前推迟到窗口开始。
- 22:00 之后推迟到下一天窗口开始。
- 申购日前一日 20:00 固定核验。
- 申购日 08:00 固定核验。
- 如果启动或恢复时已经错过固定点，则立即补做。
- 普通日期轮询加入 0–90 秒确定性抖动。
- 申购日轮询加入 0–20 秒确定性抖动。
- 普通日期和申购日间隔分别配置，默认 30/10 分钟。

同步结论：

- Unknown：尚无足够结论。
- HealthyNonempty：必要来源完整，得到有效任务。
- HealthyEmpty：必要来源完整且确认没有任务。
- DegradedCached：来源不完整或审计异常，继续使用缓存。

来源退避：

- 429/503 优先采用 Retry-After。
- 无有效服务端指示时采用 1、2、4、8、15、30 分钟本地退避。
- 本地退避带持久化抖动。
- 退避 10 分钟后允许低频健康探测，最长每 30 分钟一次。
- 探测只判断站点可达性，不绕过或提前清除 API 退避。

### 6.2 提醒与 Outbox

未确认申购时，提醒计划覆盖前一日、早间、券商受理、官方开盘、小时、上午边界、下午恢复、临近截止和最终截止。

申购后提醒：

- 设置 `post_apply_reminders_enabled` 默认开启，并可在设置页关闭。
- 设置 `listing_reminders_enabled` 默认开启，上市日提醒可独立关闭。
- 只有用户明确确认已申购后才安排；不会读取券商账户或根据时间自动推断。
- 已知中签结果公告日时，在当天 18:00 提醒用户自行查询中签结果。
- 已知缴款日时，在当天 08:30 和 14:00 提醒用户自行准备或复核资金。
- 已知上市日时，在当天 08:30 提醒用户核对交易所公告和行情软件；不读取持仓、不跟踪价格或收益，也不保证证券当日一定可正常交易。
- 撤销申购确认会在同一事务中取消后续中签、缴款和上市提醒并恢复申购提醒。
- 全量重排会回看最近 60 天，以便覆盖已经申购但仍处于中签、缴款或上市阶段的事件。
- 规划显式使用调用方传入的当前时间；已经过去的中签、缴款和上市日期不再创建提醒，当前日期只保留尚未到达的时间点。
- 正式公告解析支持“中签结果公告日”“网上投资者缴款日”“网上缴款日及截止时间”“缴款日期”等字段标签；日期未知时不猜测。

当前专用提醒窗：

- 独立于主窗口。
- 置顶显示。
- Windows 下使用 WS_EX_NOACTIVATE 和 WS_EX_TOOLWINDOW。
- 尽量定位到当前工作区右下角。
- 提供打开详情、单股二次确认和稍后处理。
- 多股同轮到期时只提供打开今日任务，不提供批量确认。

Outbox v7：

- message 字段保存 DataChanged 或复核原因。
- 事件版本变化会取消旧版本残留。
- 查询严格关联当前事件版本。
- lease 过期可跨进程重领。
- 到期判断使用 `<= now`；恢复后同一股票较低的过期申购提醒折叠到最新到期等级，旧的未确认任务不会重新创建整天的历史提醒。
- UI 显示专用提醒窗后延迟执行原生可见性检查；只有窗口可见、未最小化且与显示器工作区相交时才完成投递，否则记录失败并保留重试。
- 本地显示失败按 1、5、15、30 分钟退避；健康摘要记录失败数量、最早失败时间和最近脱敏错误，持续 15 分钟后由 Warning 升级为 Failed。
- 跨数据库重开测试覆盖入队、租约中、过期重领、显示完成和确认取消。

### 6.3 运维、备份与诊断

- Watchdog/单实例建立后立即创建主窗口和托盘；数据库升级备份、initialize、空间回收和 integrity_check 在 runtime 后台线程执行。
- 数据库未就绪时，UI 只读取内存快照；设置保存、确认、撤销、人工覆盖、第二通知测试和诊断导出返回“本地数据库仍在后台初始化”，不会以默认设置覆盖真实配置。
- SQLite 在线备份从每 128 页暂停 50 ms 调整为每 1,024 页暂停 1 ms。57,025 页旧库原参数会产生约 22.25 秒固定等待，新参数的固定让出时间低于 60 ms。
- 维护每小时独立运行，不依赖同步成功。
- 每日备份使用 SQLite 在线备份 API。
- 每次备份使用唯一临时文件，写入后执行 integrity_check，并在读写句柄上执行 `sync_all`。
- 关闭目标连接后原子提交；提交前中断会清理本轮临时文件并保留已经存在的备份。
- 失败时清理临时文件。
- 默认保留最近 7 份备份。
- 日志按日期命名，单日达到大小上限后分段，保留 14 天。
- 维护、备份和日志保留失败写入 operation_health。
- 同步运行继续记录来源、时间、结果、条数、结构指纹、响应哈希和脱敏错误，但不再持久化接口正文；migration v9 清除旧正文，空闲页达到总页数四分之一时执行 VACUUM。
- 233,574,400 bytes 的真实 schema v8 数据库副本迁移实测：UI/托盘 17 ms 呈现，备份约 0.58 秒完成，空间回收约 1.19 秒完成，最终数据库 782,336 bytes、191 页、0 空闲页。

版本升级保护：

- application-version.txt 记录最近一次成功初始化数据库的应用版本，并通过 Windows `MoveFileExW(MOVEFILE_REPLACE_EXISTING | MOVEFILE_WRITE_THROUGH)` 替换。
- 发现数据库 schema 低于当前 `LATEST_SCHEMA_VERSION` 或无法安全识别时，先创建完整备份；单纯应用版本变化不触发重复备份。
- 只有 initialize 和 integrity_check 都成功后才更新版本标记。
- 这同时保护 MSI 与便携版首次运行，不依赖安装目录。

诊断 ZIP schema v4 包含：

- 应用版本和 schema migration 版本。
- SQLite 完整性。
- 最新类型化同步结论。
- 最近同步运行。
- 来源健康与运维健康。
- 本地提醒 Outbox 和第二通知 Outbox 状态摘要。
- 最近提醒日志。
- 最近 Watchdog 崩溃报告。
- `windowsToast`：进程 AUMID、ToastNotifier 创建、NotificationSetting、开始菜单快捷方式与 AUMID 匹配、SHQueryUserNotificationState。
- 脱敏后的近期日志。

诊断 ZIP 不包含：

- 主数据库。
- 公告全文。
- Cookie、Authorization。
- URL 查询参数。
- 用户绝对路径。
- 第二通知通道 Webhook、token 或 DPAPI 加密文件内容。

### 6.4 Windows 集成与恢复

Windows 通知：

- 进程使用稳定 AUMID `StockIpoReminder.Desktop`。
- MSI 开始菜单快捷方式写入相同的 `System.AppUserModel.ID`。
- 主路径通过 WinRT `Windows.UI.Notifications` 创建 Toast；标题和正文会做 XML 转义及长度限制。
- Toast 不可用时自动回退到 Shell_NotifyIcon 托盘气泡，并设置 `NIIF_RESPECT_QUIET_TIME`。
- 便携版不会伪装成已经完成 MSI 快捷方式注册；Toast 不满足平台条件时明确降级。
- 设置页把专用提醒窗、Windows Toast、气泡回退、声音和任务栏闪烁拆成独立测试。

Windows 恢复 smoke schema v10：

- 先执行 NIM_DELETE 模拟 Explorer 丢失托盘图标，再发送真实 `TaskbarCreated` 注册消息，要求 NIM_ADD 成功 1 次且失败 0 次。
- 发送 `WM_POWERBROADCAST / PBT_APMRESUMEAUTOMATIC`、`WM_WTSSESSION_CHANGE / WTS_SESSION_UNLOCK` 和两轮 `WM_TIMECHANGE`。
- 验证防抖接受 2 次、抑制 2 次、Runtime 恢复回调 2 次；smoke 模式不执行真实联网同步，生产路径不变。
- 自动化证据覆盖消息处理和防抖逻辑；用户另已确认真实 Explorer 进程重启后托盘恢复可用。物理睡眠和 RDP 仍需独立实机验收。

第二次启动：

- 应用 Mutex 和激活消息均按数据目录隔离。
- 检测到已有监督进程或主程序时，等待启动竞态并广播激活消息。
- 已有实例在 Slint 事件循环中显示并重绘主窗口，随后用原生可见性检查写入确认日志。

当前 Windows 消息路径覆盖：

- 休眠或待机恢复。
- Windows 会话解锁。
- 系统时间变化。
- 网络从不可用恢复为可用。
- Explorer 任务栏重建。

恢复事件会防抖，并触发投递检查、时间检查和一次同步请求。

W32Time：

- 使用服务控制管理器读取 W32Time 状态。
- 服务未运行时至少显示 Warning。
- 服务状态读取失败时显示明确警告。
- 网络时间偏差超过 5 分钟仍优先显示 Failed。

Watchdog：

- 子进程退出码 0 视为正常退出，不重启。
- 非零退出视为异常退出。
- 每次异常写入 diagnostics/crashes 下的 JSON。
- 在 10 分钟窗口内按 2、10、30 秒最多重启 3 次。
- 第 4 次异常停止重启，避免崩溃循环。
- 当前矩阵已验证单次强杀恢复、连续四次崩溃停止重启，以及监督进程被强制结束后主进程继续正常运行。

### 6.5 安装与卸载

- MSI 继续使用 per-machine、Major Upgrade、可选安装目录和稳定 UpgradeCode。
- 设置页启动卸载前先用 Windows Installer API 查询本产品当前 ProductCode；未找到 MSI 注册时禁用入口并提示便携版处理方式。
- 当前 EXE 复制到 `%TEMP%` 作为短生命周期卸载 helper，主程序退出后由 helper 调用 Windows Installer。
- 默认分支只卸载程序；数据清理分支同时受 UI 二次确认、Rust 短语校验和当前用户默认数据根目录校验约束。
- 单元测试覆盖确认短语、跨用户/伪装路径拒绝和 ProductCode 规范化；schema v10 smoke/audit 覆盖 UpgradeCode 绑定、Windows Installer API、命令参数和 UI 确认短语 authoring。
- 为避免破坏当前开发机，自动 smoke 不实际卸载已安装产品；完整安装/卸载结果仍需隔离虚拟机矩阵。

### 6.6 签名发布与安全自动更新

签名构建：

- `build.bat --package --sign` 要求当前用户证书指纹或 PFX、Code Signing EKU、HTTPS RFC 3161 时间戳和 HTTPS stable 更新清单地址。
- PFX 密码只从环境变量读取；脚本通过 .NET X509Store 暂存证书，`signtool` 命令行只接收 SHA-1 查找指纹，不接收密码，结束后删除本轮新增的证书项。
- EXE 在进入便携 ZIP 和 MSI 前签名，MSI 生成后再签名；两者均执行 `signtool verify /pa /all`。
- 签名发布生成 `update-manifest.json` 和 detached CMS/PKCS#7 `update-manifest.json.p7s`，并在 release manifest 记录 signed、signerSha256、timestampUrl、updateFeedUrl 和清单文件名。
- 手动 `signed-release` CI 工作流绑定受保护 environment，从 secret 写出短生命周期 PFX，完成打包、smoke、签名更新集成和 audit 后清理 PFX 并上传包。

客户端更新：

- 默认设置允许启动时检查，但只有签名构建且检测到本产品 MSI 注册时才真正启用；`--skip-update-check` 用于隔离测试。
- 清单和 `.p7s` 使用 256 KiB 上限，MSI 使用 200 MiB 上限；所有最终重定向地址仍必须是无凭据 HTTPS。
- 清单限定 product=`StockIpoReminder`、channel=`stable`、schema v1、严格递增版本、最低 Windows Build、MSI 大小/哈希和固定签名证书；最低 Build 使用不受应用 manifest 兼容性虚拟化影响的 `RtlGetVersion` 读取。
- MSI 下载采用流式 SHA-256 和声明大小双重校验；下载完成和 helper 启动前都执行 Authenticode 系统信任与相同证书指纹复核。
- helper 等待父进程退出并给 Watchdog 收尾时间，再以 `/i ... /passive /norestart` 安装；结果写入 `diagnostics/update-last-result.json`。
- 0.3.1 临时集成报告已验证 Authenticode 文件签名、detached CMS、证书固定、MSI 哈希和篡改清单拒绝；由于未使用正式 CA，自测只接受精确的不受信测试根状态，生产系统信任仍未验收。

### 6.7 用户同意的脱敏崩溃报告

- 默认关闭。只有构建同时提供 `STOCK_IPO_CRASH_REPORT_URL` 和 `STOCK_IPO_CRASH_REPORT_PRIVACY_URL`，且两者都是绝对、无凭据 HTTPS URL 时，设置页才允许启用自动发送或明确点击发送。
- 自动发送不是静默默认行为；用户保存开启设置后，下一次启动只尝试发送一份尚未成功发送的报告。`--skip-crash-upload` 用于发布 smoke 和其他隔离测试。
- 客户端只扫描当前数据根目录的 `diagnostics/crashes` 顶层，只接受名称为 `crash-recovery-*.json`、非空且不超过 128 KiB 的普通文件；数据库、日志、公告正文和原始接口响应不进入上传选择范围。
- 上传前递归移除命令行、参数、路径、目录、用户名、主机、设备、账户、持仓、Cookie、Authorization、token、密码、secret 和 credential 等敏感键；其余字符串继续经过 `operations::redact`，数组与文本长度也有边界。
- HTTP 客户端总超时 30 秒并使用 `Policy::none()` 禁止重定向；过去滚动 24 小时最多记录 3 次尝试，失败也计数，避免无界后台重试。
- 成功报告的 SHA-256 单独保留并去重；尝试状态最多保留 100 条/30 天，成功哈希最多保留 500 个。本地状态位于 `diagnostics/crash-upload-state.json` 和 `diagnostics/crash-upload-last-result.json`。
- 当前 0.3.1 本机发布未嵌入接收端或隐私政策，release manifest 明确将两个字段记录为 null，因此不会发生真实外部上传。
- 客户端完成不等于服务上线。正式接收服务的请求认证、载荷 schema 校验、滥用防护、访问审计、数据驻留、保留期限、用户删除渠道和隐私政策仍必须在外部部署前完成。

### 6.8 第二通知通道

- 设置页可选择企业微信机器人、钉钉机器人、飞书机器人或 PushPlus，保存秘密输入、发送用户主动测试，并单独清除凭据；功能和设置均默认关闭。
- 企业微信仅接受 `qyapi.weixin.qq.com/cgi-bin/webhook/send` 且必须有 `key`；钉钉仅接受 `oapi.dingtalk.com/robot/send` 且必须有 `access_token`；飞书仅接受 `open.feishu.cn` 或 `open.larksuite.com` 的 `/open-apis/bot/v2/hook/` 路径；PushPlus 固定发送到 `https://www.pushplus.plus/send`。
- 凭据保存到 `secrets/secondary-notification.dpapi.json`，使用 Windows DPAPI 当前用户范围、禁止交互 UI，并附加产品专用 entropy；复制到其他用户或设备后不能解密。
- 远程内容只包含产品名、公开的新股简称和申购代码、提醒类型、本地到期时间、脱敏摘要与“不自动下单”边界，不发送数据库、日志、公告正文、用户/设备标识、券商信息或凭据。
- HTTP 客户端禁止重定向，总超时 15 秒，响应上限 64 KiB；HTTP 状态成功后仍校验服务商 JSON 成功代码，错误只保留脱敏摘要。
- 本地提醒进入投递租约时建立独立第二通知 Outbox 项；远程租约为 2 分钟，进程退出后可恢复，失败按 1、5、15、30 分钟退避，第 5 次失败后进入 Exhausted 并停止自动重试。
- 自动批次与发送测试共享滚动 1 小时 20 个批次配额，并在联网前预留；请求尝试保留 30 天且最多 2,000 条，Delivered、Cancelled、Exhausted 终态队列保留 90 天。
- 设置页显示待发送、重试、耗尽、最近成功和脱敏错误；关闭通道或切换服务商会取消旧服务商未完成项。
- 详细安全、凭据、消息内容和后续边界见 `docs/secondary-notifications.md`。

## 7. 尚未完成：已实现功能的必做验收

0.3.1 已完成当前工作区能够自动化闭环的提醒、时间、备份、原子状态文件、筛选和公告链路减重。以下项目仍然必须做，但原因是需要真实 Windows 环境、破坏性故障条件、正式凭据或线上部署；它们不是继续扩张产品功能。

### 7.1 真实 Windows 与安装交互

1. Windows 10 Build 19041 或更高版本实机 smoke。
2. 多显示器混合 DPI、任务栏位于不同边缘、显示器热插拔、RDP 连接和断开。
3. 真实提醒触发时验证专用窗不抢键盘焦点、按钮可操作、不会跑出工作区；同时验证持续不可见时的健康告警。
4. 安装 0.3.1 MSI 后验证 Toast 实际呈现、通知中心留存和点击激活；记录通知关闭、勿扰、专注助手和系统策略状态。
5. 真实旧版本→0.3.1 MSI 升级、升级失败回滚、修复安装、普通卸载保留数据，以及应用内确认清理当前用户数据的完整矩阵。
6. Watchdog 在 Windows 注销、关机，以及监督进程和主程序同时被结束时的行为；已通过的单次恢复、四次崩溃上限和监督进程退出矩阵不再重复测试。
7. 真实睡眠、解锁、网络恢复和长时间恢复风暴；自动化消息注入与过期提醒折叠已经完成，不再列为代码缺口。

### 7.2 破坏性和外部可靠性证据

1. 在隔离环境执行磁盘满、备份目录只读和备份期间真实强杀进程；提交前中断的自动化故障注入已经通过。
2. 在真实系统策略下验证 W32Time 停止/禁用、服务状态权限失败和代理篡改时间；无样本、单样本、正常偏差、大偏差、服务停止和读取失败的确定性矩阵已经通过。
3. 使用真实企业微信、钉钉、飞书和 PushPlus 账号完成现有第二通知通道端到端验收；DPAPI 凭据轮换、Outbox、退避、配额和清理已经有自动化证据。
4. 持续验证四个采集源与公告源的真实分页、截断、重复记录和在线契约变化；本地固定 fixture 与数量审计已经通过。
5. 对虚拟化任务列表执行真实滚动、键盘导航和无障碍实测；2,000 条固定任务的组合筛选正确性已经通过。
6. 使用更多真实公告覆盖中签、缴款和上市日提醒交互；过去日期、当前日期、日期缺失、撤销和待复核状态的确定性矩阵已经通过。

### 7.3 Windows 通知与线上可观测性

1. 安装版 Toast 的真实呈现、通知中心留存、点击激活和应用升级后 AUMID 连续性证据。
2. NotificationSetting、SHQueryUserNotificationState、专注助手、勿扰模式、无交互桌面和系统策略组合矩阵。
3. 便携版无开始菜单 AUMID 注册时的降级提示和气泡回退实机证据。
4. 为已经完成的用户同意客户端接入正式 HTTPS 崩溃报告接收端与隐私政策，并完成服务端 schema 校验、滥用防护、保留/删除政策、删除请求和安全审计。

### 7.4 安装与供应链

1. 购买或接入正式 CA Code Signing 证书，生成第一份系统信任的 EXE/MSI，并记录发布者、证书链和 SmartScreen/安全软件结果。
2. 用真实 RFC 3161 服务完成时间戳并验证证书过期后的签名可验证性；临时自签测试不作为此项证据。
3. 在受保护 CI environment 或隔离签名机接入正式密钥，完成审批、最小权限、日志脱敏、PFX 清理和签名审计；后续固定第三方 action 版本/提交。
4. 部署 HTTPS stable 更新目录，按 MSI、发布说明、`.p7s`、最后原子发布 manifest 的顺序上线；验证旧版本在线升级、断网、清单/包篡改拒绝、安装失败回滚、重启码和保留上一稳定包。
5. 在隔离虚拟机验证安全卸载：默认保留数据、明确清理、取消/UAC 拒绝、Windows Installer 失败、需要重启和旧版本兼容。

### 7.5 明确不做与未实现暂停项

以下两项已经明确排除出产品路线图，不再研究或开发：

- 扫描 PDF OCR、图像识别与版面理解。
- PDF 按页/按对象解析、外部低内存解析器或其他自动公告正文提取替代方案；旧自动 PDF 链路已经移除，不以其他解析器替代。

以下未实现能力只保留状态记录，当前暂停，也不作为 0.3.1 可靠性批次的完成条件：

- 邮件、短信、任意自建通用 Webhook，以及企业代理、自定义 CA、客户端证书、端到端加密和远端消息撤回等第二通知扩展。
- 多设备状态同步。
- 独立采集服务或采集服务拆分。
- 服务端推送、中心账号体系和设备注册。

暂停项后续若恢复，仍必须遵守“不读取券商账户、不自动交易”的产品边界；明确不做项除非用户重新改变产品边界，否则不得重新进入排期。

## 8. 后续推进顺序

### P0：产品减重，退役自动 PDF 处理（已完成）

1. 已停止为近期任务自动下载和解析公告 PDF；公告检索只保存标题、来源、发布时间和官方 URL，并保留“打开公告原文”。
2. 已移除 `lopdf`、PDF Worker 命令入口、Worker 请求/响应文件、Windows Job Object PDF 限制和对应下载临时文件链路。
3. 新同步的数据质量已收敛到结构化多来源结果、冲突和人工核验；不再产生新的 `AnnouncementVerified` 结果，同时继续兼容读取旧数据库历史记录。
4. 用户已有 PDF 默认保留，新版本不再新增公告文件；设置页本地缓存入口可用于自行查看或处理历史文件。
5. Rust/Slint 统一测试为 84/84 通过；发布打包、布局、schema v10 smoke 18/18 和 schema v2 联网同步内存检查均已通过并更新到发布基线，P0 已完整闭环。

### P1：补真实 Windows 证据

1. Windows 10、RDP、多屏混合 DPI、热插拔和真实恢复事件。
2. 提醒窗焦点、任务栏位置和持续不可见健康告警。
3. 安装版 Toast 呈现、通知点击和系统通知状态矩阵。
4. MSI 真实升级/回滚/修复/卸载，包括安全清理当前用户数据与所有失败时保留数据。
5. Watchdog 注销、关机及监督层和主程序同时失效边界。

### P2：补现有路径的破坏性或外部证据

1. 磁盘满、只读目录和真实强杀备份矩阵。
2. W32Time 真实禁用、权限和代理异常矩阵。
3. 现有四个第二通知服务商的真实账号端到端验收。
4. 来源分页、截断和契约变化的在线监控。
5. 任务列表滚动、键盘和无障碍实测，以及更多真实中签/缴款/上市公告交互。

### P3：发布供应链与线上接入

1. 正式 CA 证书、真实 RFC 3161 时间戳和系统信任签名包验收。
2. 受保护 CI/隔离签名机接入、审批、密钥清理与签名审计。
3. 部署 HTTPS stable 源，完成正式签名清单的原子发布和旧版在线升级/失败回滚矩阵。
4. 崩溃报告正式接收服务、隐私政策、保留/删除治理和线上安全验收。

### 暂停队列与明确排除项

- 暂停：邮件、短信、通用 Webhook、多设备同步、采集服务拆分和服务端推送，只保留记录，不在当前阶段排期开发。
- 明确排除：OCR、版面理解和任何新的自动 PDF 正文解析方案，不再作为后续研究候选。

## 9. 当前发布判断

0.3.1 可以作为 Windows 11 x64 的内部研究和受控发布基线：

- 源码、测试、release 构建、MSI、便携包、布局、smoke、audit 和联网内存报告均已生成。
- Watchdog 的单次强杀恢复、连续四次崩溃上限和监督进程退出矩阵已有真实隔离证据。
- 原生 Windows Toast、稳定 AUMID、MSI authoring、气泡回退和分通道自检已经实现；安装版 Toast 的真实呈现及点击激活仍待实机验收。
- Explorer 图标丢失后的 TaskbarCreated 重新注册、Windows 恢复消息与五秒防抖已有 schema v10 自动化证据，用户也已确认真实 Explorer 重启恢复可用；物理睡眠和 RDP 仍待实机验收。
- 明确确认申购后的中签查询、缴款资金和上市日提醒已经实现，但不读取账户或持仓，不推断中签、缴款或交易状态。
- 安全 MSI 卸载主路径、默认保留数据、精确确认短语和当前用户数据根目录限制已经实现；真实安装环境中的卸载/UAC/失败矩阵仍待隔离虚拟机验收。
- 数据库升级前备份改为按实际 schema 触发，备份节流参数已修正，并有 schema 变化/应用版本变化分离的回归测试。
- 233,574,400 bytes 旧库副本已验证 UI/托盘 17 ms 呈现；后台完成 v8→v9 安全备份、接口正文清理和 VACUUM 后数据库缩至 782,336 bytes。
- 当前批次已补齐过期提醒跳过与折叠、本地投递有界退避和健康错误、系统时间与跨日/回拨调度矩阵、唯一临时备份与提交前中断保护、Windows 写穿原子状态替换、DPAPI 凭据轮换及 2,000 条筛选回归。
- P0 公告链路减重已完成：当前源码不再包含 `lopdf`、PDF Worker、PDF Job Object 或自动公告下载/正文解析；同步只保存公告元数据和官方 URL，旧本地 PDF 不自动删除。
- Authenticode/RFC 3161 构建链、detached CMS 更新清单、证书固定、MSI 下载验证和更新 helper 已实现；临时签名集成已验证文件签名、CMS、哈希、证书固定和篡改拒绝。
- 当前正式发布物仍为 `signed=false`，未编译正式更新源和证书指纹，因此设置页自动更新保持关闭；正式 CA、真实时间戳、线上 HTTPS 源和在线升级/回滚仍待发布环境验收。
- 可选崩溃报告客户端已经实现默认关闭、双无凭据 HTTPS 配置、明确用户同意、二次脱敏、128 KiB 边界、禁止重定向、3 次/24 小时限流和成功 SHA-256 去重；当前正式发布未配置接收端和隐私政策，正式服务及保留/删除治理仍待外部接入。
- 企业微信、钉钉、飞书和 PushPlus 第二通知通道已实现默认关闭、官方 HTTPS 限制、DPAPI 当前用户凭据、独立 Outbox、租约恢复、最多 5 次发送、20 批次/小时和有界历史清理；它不等同于服务端推送，邮件、短信、通用自建 Webhook、多设备同步及真实服务商账号验收仍未完成。
- Windows 10、真实 MSI 升级矩阵、多屏/RDP 和安装版 Toast 交互尚未完成，因此不得把这些项目描述为已经验收；Explorer 自动重启恢复已由用户确认闭环。
- 未实现的邮件、短信、通用 Webhook、多设备同步、采集服务拆分和服务端推送按用户决定暂停并继续保留状态记录；OCR、版面理解和新的自动 PDF 正文解析方案已明确排除出路线图。

后续每次修改提醒、同步、存储、备份、Windows 集成或打包行为时，先完成统一基础门禁：

~~~text
rtk cargo fmt
rtk cmd /c build.bat --package
rtk powershell -NoProfile -ExecutionPolicy Bypass -File scripts/validate-build-layout.ps1
rtk powershell -NoProfile -ExecutionPolicy Bypass -File scripts/smoke-release.ps1
~~~

再按实际影响范围补对应专项，避免每次小改都重复执行无关的联网、签名或破坏性矩阵：

~~~text
rtk powershell -NoProfile -ExecutionPolicy Bypass -File scripts/test-signing-update.ps1
rtk powershell -NoProfile -ExecutionPolicy Bypass -File scripts/audit-release.ps1
rtk powershell -NoProfile -ExecutionPolicy Bypass -File scripts/measure-rust-memory.ps1
rtk powershell -NoProfile -ExecutionPolicy Bypass -File scripts/test-watchdog-recovery.ps1
~~~

`build.bat --package` 内部会执行 `cargo test --locked`，同一轮批量修改后不需要再无条件重复一遍完整测试；排错时优先运行受影响的定向测试，最终发布证据统一以上述流程为准。

如果只修改研究文档且不改变应用行为，可不重新生成发布物，但必须明确说明为文档-only。
