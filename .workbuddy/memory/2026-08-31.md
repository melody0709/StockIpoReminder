# 2026-08-31

## UI 布局优化（设置页第二通知通道文字截断）

- 修复 `ui/main.slint` 中设置页和健康页 `ScrollView` 内 `VerticalBox` 未水平拉伸的问题，这是导致高级选项卡片中 Toggle 标签被截断的根因。
- 给 `Toggle` 组件的文本增加 `overflow: elide`，即使空间不足也会优雅截断。
- 第二通知通道未启用时，隐藏 Webhook/Token 输入框和测试/清除按钮，减少视觉干扰并避免无意义禁用态。
- 根据输入框显隐动态调整第二通知通道卡片高度。
- 统一“节能同步”卡片内部间距为 8px，压缩“高级可选功能”卡片高度 78px→70px。
- 运行 `rtk cargo fmt` 与 `rtk cargo test`（143 passed，无失败）。
