# 2026-08-27

## 全项目深度代码审查（4 个并行审查代理 + 人工复核 + cargo test）

基线：`cargo test --locked` 101 项全部通过。项目约 17.7k 行 Rust。

### 确认的 Bug（按严重度）
- P0 `network.rs:790-798` 与 `announcement.rs:559-567` `unwrap_jsonp`：畸形 JSONP（`)` 在 `(` 前）导致 slice 越界 panic，远端可触发。需校验 `end >= start+1`。
- P1 `runtime.rs:432-446`：run_loop 任一瞬时错误（如 SQLITE_BUSY）冒泡后线程退出，后台提醒/同步永久停止，进程不死、watchdog 不重启。需循环内捕获按退避继续。
- P1 `storage.rs:644 + 126-135 + 2382`：切换/关闭第二通知渠道时 CANCELLED 行仍占 `reminder_outbox_id` UNIQUE 键，`INSERT OR IGNORE` 静默跳过，该提醒在新渠道永不发送。需 ON CONFLICT 复活或复合唯一键。
- P1 `watchdog.rs:62-63`：`write_crash_report` 失败用 `?` 传播，data_root 不可写时 watchdog 直接退出，失去重启能力。应降级为警告继续重启。
- P1 `core.rs:67`：`detect_exchange` 缺 `4` 前缀判断，43xxxx 北交所代码在 IS_BEIJING 字段缺失时误判深圳。
- P1 `announcement.rs:64-68`：SSE 官方与巨潮镜像双成功时丢弃官方结果，不取并集。
- P1 `runtime.rs:550-551`：健康摘要评估前无条件置 `last_health_date`，当日后续来源转 Failed 后告警永不发出。

### 主要 P2/P3
- `network.rs` 响应体无大小上限；Retry-After 极大值可致 chrono panic；DATA_HOSTS 混入 microsoft/cloudflare。
- `operations.rs:285` redact 正则不覆盖 UNC/正斜杠路径。
- 多处跨连接版本校验 TOCTOU（acknowledge_at、apply_manual_override）。
- 退出时若正在同步，join 最长阻塞数分钟。
- 更新检查无并发门闩；气泡点击只存最后一个 event_id。

详见会话内审查报告；修复时按上述行号定位。
