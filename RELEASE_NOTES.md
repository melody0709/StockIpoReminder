# Release notes

## 0.3.4 — 2026-09-01

### 主窗口尺寸记忆

- 用户通过拖拽调整主窗口宽高后，程序会在尺寸稳定时防抖保存，并在隐藏到托盘、关闭窗口或安全退出时再次同步保存；重新启动后从托盘打开主界面会恢复上次尺寸。
- 保存值使用逻辑像素，适配不同 Windows 缩放比例；首次显示时仍会按当前显示器工作区限制最小与最大可用尺寸并居中，避免换屏后窗口超出屏幕。
- 最大化、最小化或全屏状态不会覆盖最后一次普通窗口尺寸；损坏或超出安全范围的 `window-state.json` 会被忽略并回退默认大小，不影响提醒服务启动。

## 0.3.3 — 2026-09-01

### 默认托盘启动

- Windows 普通启动与开机自启动现在都默认直接驻留系统托盘，不再先显示或短暂闪出主界面，减少开机和手动启动时的不必要打扰。
- 双击托盘图标、选择托盘菜单、点击任务通知、从提醒窗口打开详情，以及再次启动同一数据目录的程序，仍会显式显示并刷新主窗口。
- 主窗口的工作区适配、应用图标和首次完整重绘延后到用户第一次显式打开时执行；发布 smoke 新增“不带 `--background` 的普通启动保持托盘隐藏”验证。

## 0.3.2 — 2026-09-01

### 提醒降噪与交易时段约束

- 未来申购任务的关键与普通字段变化改为静默更新列表和详情，不再在申购日前弹出 `DataChanged` 窗口；申购日的关键变化也只在当前处于官方可操作时段时提示复核。
- 取消申购日前一日预告、08:30 早间提醒、券商预受理提醒、开盘前 5 分钟提醒和午休结束前提醒。打新提醒现在只计划在上午 09:30–11:30、下午 13:00 至安全截止时间内。
- 投递层新增交易时段二次校验：午休、收市后、跨日、程序晚启动或系统休眠恢复时，已经错过的打新提醒直接取消，不再折叠后补弹；中签、缴款和上市日等独立后续提醒保持原有业务时间。
- 每日健康摘要仅在当天确有启用市场的申购任务时进入交易时段提醒；开盘前等待 09:30，午休顺延至 13:00，15:00 后不补发。无今日任务时，来源 Warning/Failed 继续显示在健康页和托盘，不再单独打断用户。
- SQLite schema 升至 v11，升级时取消旧版本遗留的前日预告、开盘前提醒、午休结束前提醒和未来任务变化通知，避免新版本首次启动仍消费旧 Outbox；完整 Rust 回归测试现为 148 项。

### 内部架构与可维护性

- 将原先超过 5,500 行的 SQLite 持久化实现按 schema、设置、事件、确认、人工覆盖、本地提醒、第二通知、同步审计、健康状态、维护与备份拆分为独立模块；数据库公开调用路径、SQL、schema 版本、连接策略和事务边界保持不变，存储测试同步按领域拆分。
- 将应用入口与 UI 实现解耦：`main` 只负责启动和 Slint 生命周期，运行时桥接、后台工作线程、设置、任务列表、详情呈现以及各类回调按职责组织；Windows 原生托盘不再以内联模块混在入口文件中。
- 将后台运行时拆分为句柄、主循环、截止时间调度、来源同步、提醒投递、健康检查、每日维护和快照模块；网络采集按来源拆分，HTTP 安全与解码策略集中管理，公告搜索按上交所、巨潮和北交所分离，Windows 集成按 Toast、单实例、窗口、Shell、自启动和清理分离。此次为保持行为不变的内部重构，不引入新的数据库迁移或配置变化。

### 提醒与状态语义修复

- 「延期发行」与「暂缓发行/暂停发行」的人工覆盖拆分为不同语义：延期发行进入可恢复的 Postponed 状态并停止该任务的申购提醒；暂缓发行与网络采集口径一致，视为中止。非人工 Postponed 仅在无来源冲突、且高可信来源同时给出正常状态和不同新申购日时恢复；同一遗留日期或低可信单源不会自动"解封"。
- 同步重规划改为使用人工覆盖生效后的事件视图，并把活跃的发行状态覆盖继承到关键数据的新版本：人工 Postponed 只能由用户修改或撤销，不会被网络新日期悄悄解除；已计划的申购提醒在覆盖期间被取消，撤销后按当前可信数据重建。
- 修复撤销人工覆盖后重规划仍残留旧覆盖值的问题：撤销操作现在按当前可信数据重算状态与提醒计划。
- 公告检索在来源未返回总数时改为满页探测分页，不再把"未知总数"误当成"只有一页"静默截断；上交所官方结果被页数上限截断且巨潮镜像同时失败时，审计警告现在同时包含两个事实。
- 专用提醒窗口完全无法显示时，本轮投递立即记入失败并按既有退避重试，不再以 2 分钟租约到期为周期无退避地无限重试。
- 提醒呈现时读取设置失败改为跳过声音、任务栏闪烁与 Toast 副作用（fail-closed），不再误用默认设置发声；主窗口刷新失败时保留未消费的数据版本号，设置读取恢复后自动重试。

### 并发与界面响应

- SQLite 设置保存、同步重规划、确认、投递失败、配额/探针领取等「先读后写」事务改为 IMMEDIATE，消除多线程下偶发的 BUSY 升级失败，后台服务自恢复重启将大幅减少。
- 保存设置、确认/撤销确认、人工覆盖的保存与撤销、诊断包生成、任务详情加载全部移至后台线程执行，界面在数据库写入或 ZIP 打包期间不再卡顿；同一操作重复提交会得到「正在处理」提示而非并发写入；退出时等待已启动的后台数据库/文件操作全部安全收尾。
- 后台运行循环每轮合并心跳写入、复用设置读取与到期查询结果，空闲时段磁盘与连接开销明显下降。
- 点击退出后，进行中的同步会尽快中止当前响应体读取（分块读取 + 取消检查），不再等待整段响应读完。

### 数据与健壮性

- 任务截止时间按「最后结束的时段」取值（最大 official_end），时段数据顺序异常时申购提醒与界面显示仍保持一致；公告分页去重统一在外层合并时执行。
- 二级通知消息按服务商 UTF-8 字节上限截断（企业微信 2048 字节、钉钉 5000 字节、飞书/PushPlus 15000 字节），超长消息不再被服务商直接拒绝；截断只在字符边界发生，省略号计入上限。
- 远端响应仅在显式声明 GBK/GB2312/GB18030 时按 GB18030 解码；无声明的非法 UTF-8 保持报错并附有限长度 hex 诊断，便于定位编码问题。
- 公告标题与 Toast 内容按 XML 1.0 合法字符过滤（保留 TAB/换行/回车），含非法控制字符的标题不再导致通知失败。
- 更新订阅 URL 与崩溃上报地址统一拒绝带密码、片段（#）等成分的地址。
- 安装/卸载模式互斥校验：`--install` 与卸载旗标等同时出现时立即报错，不再按优先级静默取其一。
- 数据库读到未知枚举值时记录去重后的诊断日志（仍按 Unknown 可恢复处理），便于发现新旧版本数据不一致。
- 每日维护新增对受控更新目录与系统 %TEMP% 中更新 helper 残留的定向清理（严格匹配命名、24 小时年龄阈值、不跟随链接）；更新下载/验签失败时即时清理临时文件；`MoveFileEx` 重启删除注册失败不再静默。
- 每日维护中单个临时文件被占用不再中断整轮清理；自动备份文件名追加唯一后缀，杜绝同毫秒碰撞导致备份失败。

## 0.3.1 — 2026-08-27

### 窗口轴向响应优化

- 宽度和高度紧凑模式不再通过同一个开关捆绑：宽度低于 1000 逻辑像素时只调整横向留白、按钮和列宽，高度低于 650 逻辑像素时只调整纵向间距、卡片高度和上下留白。窄而高或宽而矮的窗口不再同时触发两个方向的压缩。
- 最低窗口客户区由 760×460 温和提高到 800×500；紧凑高度下顶部状态行由 24px 提高到 30px，状态字体保持 13px，并增加纵向拉伸和单行省略保护，避免高 DPI 下状态文字被裁切。设置和诊断内容继续通过滚动容器完整访问。

### 深度审查修复

- 修复畸形 JSONP 反序括号可触发越界 panic；`Retry-After` 现在限制在 24 小时内并使用 checked 时间运算。业务数据和公告响应改为有界流式读取，解压后上限分别为 16 MiB 和 8 MiB；HTTPS 重定向逐跳校验白名单，业务数据域名与时间探针域名彻底分离。
- 后台运行循环不再因一次 SQLite、同步、投递或健康摘要错误永久退出，而是记录运行时健康状态并按 1/5/15/30 秒有界退避恢复。调度、投递和同步使用低频心跳识别线程停摆；Watchdog 即使因磁盘或权限问题无法写崩溃报告，也会继续执行重启策略。
- SQLite schema 最终升至 v10，第二通知 Outbox 唯一身份改为“本地提醒 + 服务商”；切换服务商、禁用后重启用均可恢复待发项，既有成功投递不会重复发送。设置与提醒重算、确认/撤销、人工覆盖均使用单事务版本检查，SQLite、DPAPI 凭据和 Run 注册写入失败时执行跨存储回滚。
- 修复 `43xxxx` 北交所代码误判；多来源质量按不同来源计数，并把申购单位、上限、市值/资金、关键日期和申购时段纳入冲突检测。上交所官方公告与巨潮沪市镜像结果取并集，SSE、CNINFO、BSE 公告按每页 100 条、最多 10 页有界检索，超过上限明确进入 Warning。
- 自动定点补做严格遵守 06:00–22:00 同步窗口，22:00 后错过的任务最早延至次日 06:00；手动同步仍可随时执行。网络采集在来源、分页、事件和时间探针边界响应退出请求，避免关闭时串行等待全部来源超时。
- 更新检查与安装分别增加并发门闩，安装下载使用每次操作独立的 UUID 临时文件；更新与卸载 helper 无法打开父进程时 fail closed。Toast 携带任务激活参数，单只提醒可路由到对应任务，多股票批次只打开任务列表；窗口恢复、单实例唤醒和托盘入口都会先刷新数据。
- 日志和诊断脱敏新增 UNC、Windows 正斜杠绝对路径及 `--data-root`；损坏的设置、会话和公告字段 JSON 不再静默退回默认值。新增回归覆盖后，固定 Rust 测试总数为 115 项。

### 真正空闲的后台调度

- 主界面和提醒事件改为由 Runtime 主动通知 Slint，不再保留每秒 UI 轮询；Windows 托盘也取消每 10 秒网络可用性探测。隐藏到托盘后，界面、托盘和后台线程都只会在状态实际变化、用户操作、Windows 恢复/时间变化或真实截止时间到来时唤醒。
- 后台任务统一为单一绝对截止时间调度器，直接睡到下一条本地提醒、第二通知重试、生命周期边界、必要同步、异常健康摘要或工作日维护。到期提醒不再依赖 10 秒轮询，系统从睡眠恢复后会立即处理已经逾期的项目。
- 健康同步确认当天无任务后，当天停止周期同步；周六、周日没有已知提醒时跳到下一个工作日。申购任务确认后停止申购日周期核验；已知中签、缴款或上市日期时直接睡到准确提醒，后续日期尚未公布时每个工作日最多进行一次发现同步。来源覆盖异常仍按持久化退避执行必要重试。
- 移除跟随 UI 刷新的高频心跳，调度/投递仅保留每分钟一次的低频合并心跳用于识别线程停摆；健康判断同时识别正常长时间休眠和真实心跳过期，健康页显示“后台正常休眠”及下一次唤醒时间。健康且无任务时不再写每日摘要去重记录，也不弹出无内容价值的摘要。

### 写盘、维护与备份减负

- 数据库维护改为每个工作日最多一次，周末无任务时不运行；各保留策略先用只读 `EXISTS` 判断，确认存在过期数据后才执行删除。运维健康状态只有状态或错误实际变化时才更新，重复健康结果不再写盘。
- 自动备份使用业务状态指纹，仅在事件、设置、确认、提醒队列、人工覆盖或公告等实际业务内容变化后生成。新的自动备份最多保留 7 份并限制为 512 MiB；升级前备份和本次优化前已有的历史备份不纳入自动删除。备份结束或中断后同时清理临时数据库及其 WAL/SHM/journal 边车文件。
- 同步结果未改变任务字段时不再重写 `ipo_events`、重新规划 Outbox 或刷新字段来源抓取时间，减少活跃日必要同步的 WAL 放大；来源审计、失败记录和真正的数据变化仍完整保留。
- 设置页把原“普通日期自动同步间隔”明确为“来源异常重试间隔”，把申购日间隔明确为“当日未确认任务核验间隔”；健康空结果、已确认任务和周末静默不再受这两个间隔驱动。

### 独立提醒窗口首次显示

- 修复程序驻留托盘、主界面尚未打开时，后台直接弹出的每日健康摘要或到期提醒可能出现内容泛白、残缺的问题。独立提醒窗口完成无激活显示和工作区定位后会立即执行全窗重绘，并在原生窗口与软件渲染器完成切换后补绘一次；窗口仍保持置顶且不抢键盘焦点。

### 托盘后台资源占用

- 主窗口隐藏后不再每秒重新读取设置、今日任务、未来任务和完整健康状态，也不再反复重建 Slint 列表模型；运行时快照增加变更序号，只有状态实际变化或窗口重新显示时才刷新完整界面。列表文字、市场和状态筛选改为用户编辑或选择后即时刷新。
- 到期提醒仍保持 10 秒检查精度；生命周期校准、健康摘要、运行时快照和心跳拆分为独立低频任务。调度与投递心跳合并为每分钟一次 SQLite 事务，空的本地/第二通知队列在只读预检后直接返回，不再创建无意义的写事务。
- SQLite 的 WAL 模式只在数据库初始化时设置，不再随每次短连接重复执行；第二通知历史由每小时维护兜底清理，无待发送项时不再进入清理事务。托盘图标状态没有变化时也不再重复提交原生更新。

### 公告链路与程序减重

- 停止自动下载和解析正式公告 PDF；同步只保存公告标题、来源、发布时间和官方 URL，详情页继续支持用默认浏览器打开原文。
- 移除 `lopdf`、同 EXE PDF Worker、Windows Job Object PDF 内存限制以及 Worker 请求、响应和下载临时文件链路；结构化同步不再产生 PDF 子进程或新增本地公告文件。
- 新任务的数据质量由 Eastmoney、SSE、CNINFO、BSE 的结构化多来源结果、来源冲突和人工核验状态决定，不再产生新的“正式公告已核验”结果。旧数据库中的历史公告记录和本地 PDF 仍可读取和打开，不会被升级自动删除。
- OCR、版面理解和新的自动 PDF 正文解析方案明确排除出产品路线图；尚未公布的字段保持待公布，由后续结构化同步补齐。

### 结构化数据采集减负

- 东方财富候选查询不再无过滤读取 5,622 条历史记录中的第一页 500 条。现在按申购日限定过去 60 天至未来 60 天，只请求提醒实际使用的字段，并按过滤后的总页数读取；声明数与本轮明细数的审计恢复为同一查询范围，消除“全历史总数对比第一页”的持续假警告。
- 上交所候选查询改用其“新股发行一览”页面当前使用的 `isIssue=1` 和单页缓存参数，不再每轮读取 1,475 条历史发行记录；北交所查询增加相同的 120 天申购日期窗口，不再读取 347 条历史记录。巨潮当前 IPO 小结果集和三个已按股票、日期窗口检索的公告元数据源保持不变。
- 东方财富明确返回“数据为空”时按健康空结果处理；东方财富、上交所和北交所均保留有界分页，最多 5 页，超过上限会通过数量审计进入 Warning。多页响应现在合并为可解析的结构快照，来源 schema 指纹不再因 JSONP 文本拼接失真。

### 启动速度与数据库瘦身

- 主窗口和托盘不再等待旧数据库备份、schema 迁移、完整性检查或空间回收；数据库准备改在后台完成，未就绪期间所有数据库读写入口都有门禁，真实设置只在就绪后加载，避免默认值覆盖已有配置。
- 修复 SQLite 在线备份每复制 128 页就固定等待 50 ms 的参数错误；230 MiB 数据库原本会因此产生约 22 秒纯等待，现在改为每 1,024 页让出 1 ms，同时保留唯一临时文件、完整性检查、`sync_all` 和最终提交。
- 升级前备份改为根据实际 schema 版本触发，普通应用版本变化不再重复整库备份。数据库瘦身迁移引入 schema v9：同步审计继续保留来源、时间、条数、状态、结构指纹、响应哈希和错误，但不再保存提醒功能不使用的接口正文；旧正文会在迁移中清除并按空闲比例压缩数据库。本版本最终 schema 为 v10，第二通知 Outbox 迁移见“深度审查修复”。
- 使用 233,574,400 字节的 schema v8 数据库副本实测：界面和托盘 17 ms 呈现，迁移后数据库为 782,336 字节、接口正文 0 条、空闲页 0；迁移前备份仍通过完整性门禁保留。
- 设置页新增“本地缓存与数据”入口，可直接打开当前数据目录；同时扩大提醒通道卡片的自适应高度，完整显示任务栏闪烁测试和通道测试状态，不再被第二通知通道区域截断。
- 第二通知、安全自动更新和脱敏崩溃报告收纳到默认折叠的“高级可选功能”，未配置状态不再显示成警告；新安装的启动时更新检查默认关闭，核心提醒和同步设置保持直接可见。

### 已有功能可靠性闭环

- 本地提醒在程序恢复或长时间离线后，会把同一股票已过期的较低申购提醒折叠到最新到期层级，避免一次弹出整天的历史提醒；可见性失败改为 1、5、15、30 分钟有界退避。
- 本地提醒持续失败会进入健康状态：短期重试显示 Warning，持续 15 分钟后显示 Failed，并在健康页和诊断包提供脱敏后的最近错误。
- 申购后提醒规划显式接收当前时间，跳过已经过去的中签、缴款、上市日期；补充旧申购任务、公告日期缺失、当日早晚时间点和待复核状态的确定性矩阵。
- SQLite 备份临时文件改为每次唯一名称，提交前强制刷新文件并在失败时清理；新增“提交前中断”故障注入，保证既有备份不受影响。
- 应用版本标记、第二通知通道 DPAPI 凭据和崩溃上传状态使用 Windows 原子替换并请求写穿，不再先删除已提交文件；新增凭据轮换回归。
- 系统时间判断拆出确定性矩阵，覆盖无样本、单样本、正常偏差、超过五分钟偏差、W32Time 停止和状态读取失败；同步调度补跨日窗口和系统时钟回拨输入。
- 增加 2,000 条固定任务样本的筛选回归，验证简称、市场和状态组合不会漏项。

### 范围说明

- 本版本不开发 OCR、多设备同步、采集服务拆分、服务端推送、邮件、短信或通用 Webhook；其中 OCR 和新的自动 PDF 正文解析方案已明确不做。

## 0.3.0 — 2026-08-26

### 加密的第二通知通道

- 设置页新增企业微信机器人、钉钉机器人、飞书机器人和 PushPlus；默认不启用，不登录服务商账号，也不接触券商账户、持仓或委托状态。
- 企业微信、钉钉和飞书 Webhook 只接受对应官方域名、固定路径和无凭据 HTTPS URL；PushPlus 使用固定 HTTPS 接口。客户端禁止 HTTP 重定向、使用 15 秒总超时、限制 64 KiB 响应，并同时校验 HTTP 状态与服务商 JSON 成功代码。
- Webhook 或 token 通过 Windows DPAPI 绑定当前用户加密，单独保存到 `secrets\secondary-notification.dpapi.json`；不会写入 SQLite、日志、诊断 ZIP、命令行或发布清单。设置页支持明确发送测试和清除凭据。
- 第二通道复用提醒产生时机但使用独立持久化 Outbox，不会因为远程服务失败而阻塞桌面提醒；失败按 1、5、15、30 分钟退避，单条最多 5 次。
- 自动批次与用户测试在发送前统一预留配额，滚动 1 小时最多 20 个请求；进程异常退出后未完成的租约可恢复，达到上限后在设置页显示明确状态。
- 尝试记录保留 30 天且最多 2,000 条，已完成、取消或耗尽的远程队列项保留 90 天；诊断 ZIP schema 升至 v4，只记录脱敏状态和计数，不包含加密文件或明文凭据。

### 验证与发布门禁

- SQLite schema 升至 v8，新增第二通知 Outbox、请求配额和恢复状态；新增 7 项定向测试，覆盖官方 URL、服务商响应、DPAPI 往返、诊断排除、持久化重试、5 次上限、小时配额和历史清理，总回归测试增至 74 项。
- Windows smoke schema 升至 v10，增加 DPAPI、禁止重定向、官方域名、持久化队列、重试/配额及设置 UI authoring 门禁；release audit 要求该门禁通过。

## 0.2.9 — 2026-08-26

### 用户同意的脱敏崩溃报告

- 设置页新增可选崩溃报告共享；默认关闭，只有构建同时配置 HTTPS 接收地址和 HTTPS 隐私政策时才可启用自动发送或明确点击发送最近报告。
- 客户端只读取 `diagnostics/crashes` 顶层、不超过 128 KiB 的 Watchdog JSON；上传前再次移除命令行、路径、目录、用户名、主机、设备、账户、持仓、Cookie、Authorization、token、密码、secret 和凭据字段，并对剩余文本继续脱敏。
- 上传载荷不包含数据库、日志、公告正文、原始接口响应、用户数据目录或稳定设备标识；HTTP 重定向被禁用，接收地址和隐私政策均必须是不含凭据的 HTTPS URL。
- 本地状态记录报告 SHA-256、尝试时间和成功状态：同一报告成功后不重复发送，过去 24 小时最多尝试 3 次，失败不会进入无界重试。
- 构建和受保护签名 CI 可通过 `STOCK_IPO_CRASH_REPORT_URL` 与 `STOCK_IPO_CRASH_REPORT_PRIVACY_URL` 接入正式服务；当前本机发布仍未配置接收端，所有报告只保存在本地。

### 验证与发布门禁

- 新增 4 项单元测试，覆盖双 HTTPS 配置、敏感字段/路径脱敏、目录和大小范围、成功去重以及滚动 24 小时限流；固定回归测试增至 67 项。
- Windows smoke schema 升至 v9，增加用户同意、二次脱敏、无重定向和限流主路径的 authoring 门禁；release manifest 与 audit 明确记录崩溃服务和隐私政策是同时配置还是同时关闭。
- 内存测量只统计受测主进程及其直接同路径 PDF Worker，不再把并行 smoke 或 Watchdog 的同名进程误计为 Worker。

## 0.2.8 — 2026-08-26

### Authenticode 与签名发布链

- `build.bat --package --sign` 会要求具有 Code Signing EKU 的当前用户证书或短生命周期 PFX、HTTPS RFC 3161 时间戳和 HTTPS 稳定版更新源；EXE、MSI 完成签名后由 `signtool verify /pa /all` 复核。
- PFX 密码只从环境变量读取，PFX 临时导入当前用户证书存储后按指纹签名，完成后删除导入项；仓库不保存证书、密码或私钥。
- 发布清单新增签名状态、签名证书 SHA-256、时间戳 URL、更新源和更新清单字段；未配置正式证书的构建继续明确记录 `signed: false`。
- 新增手动触发的受保护 CI 签名工作流和独立发布说明，便于接入审批环境及 CI secrets。

### 安全自动更新

- 设置页新增启动时检查、手动检查和明确下载安装入口；便携版不会静默转换为安装版，只有已注册的 MSI 安装版可使用应用内更新。
- 正式签名构建生成 `update-manifest.json` 和 detached CMS/PKCS#7 签名；客户端固定编译时证书 SHA-256，只接受 HTTPS stable 通道和严格递增的 `x.y.z` 版本。
- MSI 下载采用大小上限和流式 SHA-256 校验，安装前再次执行 Windows Authenticode 信任与证书指纹验证；任一步失败都不会启动 Windows Installer。
- 更新 helper 等待主程序和 Watchdog 收尾后使用 `/passive /norestart` 安装，安装结果写入本地诊断；数据库继续在新版本首次启动、schema 迁移前执行完整性校验备份。
- 新增临时自签名证书集成测试，覆盖 Authenticode、detached CMS、证书固定、安装包哈希和篡改清单拒绝；正式 CA 证书、线上 HTTPS 源和 SmartScreen 信誉仍需发布环境验收。

## 0.2.7 — 2026-08-26

### 安全卸载与数据保留

- 设置页在检测到本产品 MSI 注册时提供卸载入口；普通卸载继续默认保留当前用户的数据库、设置、公告缓存、日志和备份。
- 可选的数据清理要求用户准确输入 `删除当前用户数据`。当前用户进程复制临时卸载助手并调用 Windows Installer；只有 MSI 成功卸载后才删除已经严格校验的 `%LocalAppData%\StockIpoReminder`，失败时保留全部数据。
- 卸载助手通过稳定 UpgradeCode 查询本产品 ProductCode，不接受任意 MSI 标识，也不枚举或删除其他 Windows 用户的数据目录；便携版不会伪装成可由 MSI 卸载。

### 上市日提醒

- 用户明确确认已申购后，若公开数据给出上市日期，程序会在上市日 08:30 生成本地提醒；该通道可在设置页独立关闭。
- 上市提醒不读取持仓、不追踪价格或收益，也不把公开日期描述为证券一定可以正常交易的保证；用户仍需核对交易所公告和行情软件。
- 全量提醒重排的历史窗口扩大至最近 60 天，避免申购日至上市日间隔较长时因重启或设置变更漏排上市提醒。

### 任务列表性能与筛选

- 今日任务和未来 60 天增加简称/股票代码/申购代码文本筛选，以及市场、确认状态组合筛选。
- 两个任务列表改用 Slint `ListView` 虚拟化，只实例化可见卡片，并为卡片补充列表项无障碍标签。
- 空列表会区分“当前没有任务”和“有任务但筛选后无结果”，并显示当前结果数与总数。

### Windows 恢复与发布证据

- Windows 恢复 smoke 会先模拟 Explorer 丢失托盘图标，再发送真实 `TaskbarCreated` 消息并验证 `Shell_NotifyIcon` 重新注册成功。
- 同一 smoke 会发送休眠恢复、会话解锁和两次时间变化消息，验证五秒防抖只触发两次 Runtime 恢复回调；生产模式仍按原路径执行联网恢复同步。
- 发布 smoke schema 升至 v7，并增加安全 MSI 卸载 authoring 门禁；真实 Explorer 进程重启、睡眠、RDP 和完整 MSI 卸载矩阵仍需实机验证。

## 0.2.6 — 2026-08-26

### Windows 通知与交互可靠性

- 系统通知改为原生 Windows Toast 优先；MSI 开始菜单快捷方式注册稳定的 `StockIpoReminder.Desktop` AUMID，Toast 权限、注册或呈现状态异常时自动回退 `Shell_NotifyIcon` 托盘气泡。
- 设置页把置顶提醒窗、Windows Toast、托盘气泡回退、声音和任务栏闪烁拆成独立测试，并显示 Toast 权限、开始菜单 AUMID 和当前 Windows 通知呈现状态。
- 自测试与诊断 ZIP 增加 Toast/AUMID 状态；发布 smoke 增加进程 AUMID、MSI ShortcutProperty authoring 和通知诊断门禁。
- 第二次启动同一数据目录时会广播唤醒已有实例，并在主窗口实际可见后写入确认日志；Watchdog 启动竞态会等待主程序单实例锁建立。
- Outbox 只有在专用提醒窗确实可见、未最小化且与显示器工作区相交后才完成；可见性确认失败会保留错误并延后重试。

### 中签查询与缴款资金提醒

- 用户确认已申购后，可按公开数据中的中签结果日于 18:00 提醒查询，并在缴款日 08:30、14:00 提醒登录券商核对中签和资金。
- 这些提醒只使用公开日期，不读取券商账户、不推断是否中签、不判断缴款是否成功，也不把本地提示点描述为交易所或券商截止时间。
- 正式公告字段解析增加中签结果公告日和网上缴款日期；关闭该设置或撤销申购确认会事务性撤销对应后续提醒。

### 恢复与发布验证

- Windows Time 自测试记录 W32Time 查询和运行状态；专用提醒窗 smoke 自动验证位于工作区且不抢前台焦点。
- 新增完整 Watchdog 恢复矩阵脚本，覆盖单次强杀恢复、2/10/30 秒退避、10 分钟内第 4 次停止重启，以及 Watchdog 被结束后主程序继续运行。
- 发布 smoke schema 升至 v5，发布审计要求第二次启动唤醒、提醒窗不抢焦点、W32Time、Toast 诊断和 MSI AUMID authoring 全部通过。

## 0.2.5 — 2026-08-26

### 提醒可靠性与 Windows 交互

- 新增独立非模态置顶提醒窗，采用不抢键盘焦点的 Win32 扩展样式并定位到工作区右下角；提醒不再强制恢复主窗口。
- 同一轮按股票聚合到期提醒：单股多个等级合并为一条，多股批量展示但不提供“一键全部确认”，仍需逐只核对和二次确认。
- Outbox 只有在提醒窗实际进入显示流程后才完成 lease；完成和失败写入都要求有效租约，事件版本变化会取消旧版本待投递、租约中和失败记录。
- 非关键字段变化新增一次性 `DataChanged` 通知；关键变化继续触发待复核并发送即时复核提示。
- 托盘可直接打开今日任务、未来 60 天和日志目录；Explorer 广播 `TaskbarCreated` 后会重新注册托盘图标。

### 同步、来源与运维健康

- 自动同步限制在 06:00–22:00，并增加申购日前一日 20:00、申购日 08:00 定点核验、错过补做以及普通日 0–90 秒/申购日 0–20 秒确定性抖动。
- 同步结论改为 `Unknown`、`HealthyNonempty`、`HealthyEmpty`、`DegradedCached` 四态；只有必要来源完整且计数/明细/身份校验一致时才允许得出健康空结果。
- 长退避增加 10 分钟后开始、最长每 30 分钟一次的低频站点健康探测；探测不会提前解除 API 退避。
- 每小时独立执行维护与每日备份；SQLite 备份使用临时文件、完整性检查和原子提交，维护、备份、日志保留失败都会进入运维健康状态。
- 日志改为按日期保存、单日按大小分段并保留 14 天；诊断 ZIP schema v2 增加应用/迁移版本、同步结论、近期同步、来源与运维健康、Outbox、提醒和崩溃报告摘要。

### 异常恢复、时间与升级保护

- 新增同 EXE Watchdog：主程序异常退出后按 2、10、30 秒退避重启，10 分钟内最多重启 3 次；正常安全退出不会重启，崩溃循环会停止并写入脱敏 JSON 报告。
- 系统时间诊断除 Microsoft/Cloudflare HTTPS 时间样本外，同时检查 Windows Time（W32Time）服务状态；服务未运行或状态不可读会明确降级为警告。
- 新版本首次打开旧数据库时，会在 schema 迁移前创建并校验 SQLite 备份；备份失败会中止启动，避免在没有可恢复副本时继续迁移。

### 验证

- Rust/Slint、固定 fixture、SQLite、同步调度、Outbox 恢复、备份和升级保护回归测试共 51 项通过。

## 0.2.4 — 2026-08-26

### 漏报防护与确认复核

- 扩大关键变化判定范围：除申购代码、日期、价格和发行状态外，申购上限、申购单位、所需市值/资金、官方申购时段、资金模式及时间敏感规则变化也会递增事件版本。
- 已确认任务遇到上述变化时进入“已确认但需复核”，恢复对应版本提醒，并在确认记录中保存具体变化字段；券商受理起点、安全截止、来源名称或发布时间等非官方元数据变化不会误触发复核。
- 同步结束结论增加必要来源覆盖检查。只有东方财富与所有启用市场对应来源在本轮均成功时，零任务结果才显示“今日无新股”；覆盖不完整时保留现有今日任务并写入独立同步审计结论。

### 同步调度与退避

- 自动同步拆分为普通日期和申购日两档，默认分别为 30 分钟和 10 分钟；设置页可独立配置 5 分钟至 7 天，申购日间隔不得大于普通日期间隔。
- 旧版默认 24 小时配置自动迁移到 30/10 分钟；旧版单一自定义间隔保留普通日期值，并把申购日间隔收紧到不超过 10 分钟。
- HTTP 429/503 现在解析秒数或 HTTP 日期格式的 `Retry-After`，并优先采用服务端退避时间（最长 24 小时）；无服务端指示时继续使用 1/2/4/8/15/30 分钟指数退避并加入最多 10% 抖动。
- 来源健康过期阈值同步采用当前普通日或申购日间隔，发布 smoke、审计和联网内存脚本默认从 `Cargo.toml` 读取版本，并拒绝显式传入不一致版本。

### 验证

- 固定 fixture、SQLite 迁移与状态、关键字段复核、双频率调度、来源覆盖结论、退避解析和缺失字段保留回归测试增至 35 项。

## 0.2.3 — 2026-08-26

### 申购确认

- “确认已申购”现在仅在任务所列申购日当天可用；未来 60 天列表不再允许提前确认。
- 存储层同步校验申购日期和任务状态，即使绕过界面也不会把未来、过期、暂停或终止的任务写成已确认并取消提醒。
- 增加未来任务拒绝确认的回归测试；升级后若发现旧版本遗留的未来已确认记录，调度刷新会自动撤销无效确认、恢复提醒并回到“待申购日”。

### 界面与同步设置

- 增加标题栏版本标识，并将标题与同步状态拆分为独立的不可压缩行；状态文字不再使用高 DPI 下会切断中文字形的省略裁剪。
- 增加 Windows Per-Monitor V2 DPI 清单，并为宽度低于 1000 或高度低于 650 逻辑像素的窗口启用紧凑布局；最低客户区调整为 760×460，任务卡片、诊断页、设置时间输入和详情人工覆盖区域会按可用空间收缩或换行。
- 自动同步间隔改为可自定义的分钟/小时设置，范围为 5 分钟至 7 天；保存后持久化并由后台定时器按新间隔执行。

### 公告源与健康诊断

- 修复 `bse-announcement`、`cninfo-announcement` 和 `sse-announcement` 的错误失败状态：公告运行结果现在原子写入同步记录、来源健康和退避状态，并按来源整轮汇总部分成功与全部失败。
- 补齐上交所公告查询、巨潮公告查询及北交所无 `www` 域名白名单，并校验请求重定向后的最终地址。
- 沪市公告保留上交所官方检索，同时增加巨潮资讯沪市公告镜像；上交所直链遇到 JavaScript 验证时可由镜像 PDF 接管，单一路径异常会显示可诊断的警告。
- 公告候选按正式发行公告优先排序，过滤律师意见、核查报告、保荐书、批复和招股书附录；取得高置信度正式证据后停止下载冗余大附件。
- 公告下载只接受具有 `%PDF-` 签名的文件，明确拒绝 HTML、WAF 和 JavaScript Cookie 挑战页，不再将其保存或解析为公告证据。
- 来源新鲜度阈值与配置的自动同步间隔对齐；无相关近期任务的公告来源不再因未被调用而误报失败，历史正式公告验证状态也不会被临时上游故障无故降级。

### 安装与开机自启动

- 开机自启动从计划任务改为当前用户 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` 注册项，并在程序启动时自动校准设置、清理旧计划任务。
- 发布安装包改为标准 x64 MSI，默认安装到 Program Files，安装界面允许更改目录，升级时会恢复上次选择的位置。
- MSI 使用稳定 UpgradeCode、按版本生成 ProductCode、Major Upgrade、开始菜单快捷方式和 Windows Installer 事务回滚；普通卸载继续保留用户数据。
- 生成目录统一收口到 `build/`：Cargo 中间产物、唯一运行载荷、测试/诊断证据、日志和版本化发布包各自归入固定子目录，并增加 `build.bat` 与布局校验。

## 0.2.2 — 2026-08-25

### 最终发布与验收

- 统一 Cargo、运行时 User-Agent、发布脚本和文档版本为 0.2.2，并重新生成安装包与便携包。
- 发布审计保留 `source.rust-only` 门禁，确保活动源码树不存在 C#、WPF、`.csproj` 或 `.sln` 遗留，正式构建与运行不依赖 .NET Runtime。
- 修复联网同步内存脚本的完成状态判定，确保同步结束后再检查主进程、短生命周期 PDF Worker 和临时文件残留。
- 完整继承 0.2.1 的托盘隐藏恢复全量重绘修复与纯 Rust 根目录结构。

## 0.2.1 — 2026-08-25

### 修复

- 修复 Slint 软件渲染窗口隐藏到托盘后，双击托盘恢复时偶发只重绘局部区域、其余客户区残留桌面背景的问题。
- 所有窗口恢复入口统一执行整窗背景失效，并在原生窗口完成显示后补一次全量重绘；覆盖托盘双击、托盘设置、通知点击、提醒弹窗和退出确认。
- 用户实机验证多次隐藏与托盘恢复正常；Rust 固定 fixture 与存储回归测试继续保持 16/16 通过。

### 仓库整理

- Rust 正式实现从历史 `prototypes/stock-ipo-reminder-rust` 路径迁移到仓库根目录的标准 Cargo 布局。
- 固定测试样本迁移到 `tests/fixtures`，应用图标迁移到 `assets`。
- 删除旧 C#/.NET/WPF 工程、测试、诊断/安装工具和本地 .NET SDK；正式源码、测试与发布链只保留 Rust。

## 0.2.0 — 2026-08-25

正式运行版本完全迁移到 Rust，不再依赖 .NET Runtime。该版本发布时旧 C#/.NET 源码仍作为历史基线保留，随后已在 0.2.1 仓库整理中移除。

### Rust 正式版

- 使用 Slint 提供任务列表、详情、设置、确认/撤销和人工字段覆盖界面。
- 使用 `windows-rs` 实现原生托盘、气泡通知、声音、单实例、Working Set trim 和登录计划任务。
- 迁移 Eastmoney、SSE、CNINFO、BSE 四来源同步、多来源裁决、来源健康和 1/2/4/8/15/30 分钟指数退避。
- 兼容旧版 SQLite schema，并迁移事件版本、提醒 Outbox、确认记录、人工覆盖、健康摘要、备份、维护和诊断包。
- 同一 Rust EXE 提供短生命周期 PDF Worker；采用 64 KiB 流式下载、32 MiB PDF/4 MiB HTML 上限、20 页、256,000 字符和 60 秒超时。
- 自动同步调整为启动时一次，之后每 24 小时一次；保留手动同步。同步完成后释放响应和正文缓存并执行 Working Set trim。
- 安装、升级、回滚、普通卸载保留数据、便携包和发布审计全部改用单个 Rust EXE。

### 稳定性与验收

- 修复新事件首次同步时公告早于事件落库导致的 SQLite 外键失败，并增加回归测试。
- 修复“确认已申购 → 撤销确认”后事件状态被重新写回已确认的问题；撤销、事件恢复和提醒重排现在处于同一 SQLite 事务。
- 同步错误不再被静默吞掉；失败会写入脱敏日志并恢复界面同步状态。
- 修复后台启动和窗口关闭生命周期：`--background` 不再短暂重显，点击右上角关闭后事件循环继续驻留托盘；只有界面“安全退出”、托盘退出或测试超时参数才结束程序。
- 应用 EXE、Slint 标题栏和系统托盘统一嵌入正式多尺寸图标；标题栏图标延迟到事件循环启动后安装，失败不会阻断程序启动。
- 固定 fixture 与存储回归测试 16/16 通过；真实联网同步完成 45 条近期候选、24 个事件、7 份公告，四来源成功、零来源失败。
- 最新正式 EXE 隔离同步中，主进程峰值为 13,701,120 bytes Private Bytes / 33,443,840 bytes Working Set；包含短生命周期 Worker 的进程族峰值为 25,030,656 / 60,645,376 bytes。
- 同步完成 5 秒后主进程为 13,701,120 bytes Private Bytes / 20,795,392 bytes Working Set；7 份 PDF 均已落盘，PDF Worker、Worker 请求/响应和下载临时文件残留均为 0。
- release smoke 空闲值为 5,095,424 bytes Private Bytes / 29,802,496 bytes Working Set；0.2.0 单 EXE 约 14.6 MiB，便携 ZIP 约 7.4 MiB，发布包不包含 `.dll`、`.deps.json` 或 `.runtimeconfig.json`。

### 已知限制

- 不连接券商、不自动下单，也无法验证券商是否受理委托。
- 发布物尚未进行 Authenticode 代码签名。
- Windows 11 Build 26200 已完成 smoke；Windows 10 2004+ 的最终支持声明仍需要对应系统证据。

## 0.1.1 — 2026-08-24

### 改进

- 更新应用、窗口标题栏、任务栏和系统托盘图标为新版“市场哨兵”设计。
- 图标包含 16–256 px 多尺寸资源，改善任务栏和高 DPI 环境下的辨识度。
- 托盘右键菜单显示当前版本号，并固定从底部任务栏上方弹出。

## 0.1.0 — 2026-08-24

首个 Windows 预发布候选版本。

### 功能

- 沪市、深市和北交所新股申购任务发现与多源核验。
- 常驻托盘、逐只人工二次确认、撤销确认和关键字段变化后重新确认。
- 每小时到每 2 分钟的分级提醒、午间恢复和安全截止处理。
- SQLite 持久化、提醒 Outbox、重启/休眠恢复、来源健康和每日摘要。
- 正式公告下载、哈希、字段证据、人工覆盖和待人工核验状态。
- 数据清理、SQLite 在线备份、日志轮转、脱敏诊断包和系统时间检查。

### 数据可靠性

- CNINFO 独立 Cookie 会话、预热、来源内节奏和健康空结果处理。
- SSE/CNINFO/BSE 公告正文与 `%PDF-` 文件签名校验。
- HTML/WAF 伪 PDF 在落盘前拒绝，并受控转入人工核验。
- 严格联网诊断覆盖四个主 Collector、预期公告 Provider、近期公告覆盖、PDF 签名和 SQLite 完整性。

### 发布

- 自包含 `win-x64` 便携 ZIP。
- 不需要管理员权限的每用户安装器。
- 升级前数据库一致性备份和程序目录原子替换。
- 普通卸载默认保留数据；删除数据必须显式二次确认。
- 发布清单和 SHA-256 校验文件。

### 已知限制

- 不连接券商、不自动下单，也无法验证券商是否受理委托。
- 首版发布物未进行代码签名，Windows 可能显示未知发布者提示。
- 当前 Windows 11 Build 26200 验证可用；Windows 10 2004+ 的最终支持声明仍需要对应系统 smoke 证据。
