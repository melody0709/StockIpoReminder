# Release notes

## 未发布 — 2026-08-26

### 界面与同步设置

- 增加标题栏版本标识，并将标题与同步状态拆分为独立的不可压缩行；状态文字不再使用高 DPI 下会切断中文字形的省略裁剪。
- 增加 Windows Per-Monitor V2 DPI 清单，并为宽度低于 1000 或高度低于 650 逻辑像素的窗口启用紧凑布局；最低客户区调整为 760×460，任务卡片、诊断页、设置时间输入和详情人工覆盖区域会按可用空间收缩或换行。
- 自动同步间隔改为可自定义的分钟/小时设置，范围为 5 分钟至 7 天；保存后持久化并由后台定时器按新间隔执行。

### 公告源与健康诊断

- 修复 `bse-announcement`、`cninfo-announcement` 和 `sse-announcement` 的错误失败状态：公告运行结果现在原子写入同步记录、来源健康和退避状态，并按来源整轮汇总部分成功与全部失败。
- 补齐上交所公告查询、巨潮公告查询及北交所无 `www` 域名白名单，并校验请求重定向后的最终地址。
- 沪市公告保留上交所官方检索，同时增加巨潮资讯沪市公告镜像；上交所直链遇到 JavaScript 验证时可由镜像 PDF 接管，单一路径异常会显示可诊断的警告。
- 公告候选按正式发行公告优先排序，过滤律师意见、核查报告、保荐书、批复和招股书附录；取得高置信度正式证据后停止下载冗余大附件。
- 公告下载只接受具有 `%PDF-` 签名的文件，明确拒绝 HTML、WAF 和 JavaScript Cookie 挑战页，不再将其保存或解析为公告证据。
- 来源新鲜度阈值与配置的自动同步间隔对齐；无相关近期任务的公告来源不再因未被调用而误报失败，历史正式公告验证状态也不会被临时上游故障无故降级。

### 安装与开机自启动

- 开机自启动从计划任务改为当前用户 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` 注册项，并在程序启动时自动校准设置、清理旧计划任务。
- 发布安装包改为标准 x64 MSI，默认安装到 Program Files，安装界面允许更改目录，升级时会恢复上次选择的位置。
- MSI 使用稳定 UpgradeCode、按版本生成 ProductCode、Major Upgrade、开始菜单快捷方式和 Windows Installer 事务回滚；普通卸载继续保留用户数据。
- 生成目录统一收口到 `build/`：Cargo 中间产物、唯一运行载荷、测试/诊断证据、日志和版本化发布包各自归入固定子目录，并增加 `build.bat` 与布局校验。

## 0.2.2 — 2026-08-25

### 最终发布与验收

- 统一 Cargo、运行时 User-Agent、发布脚本和文档版本为 0.2.2，并重新生成安装包与便携包。
- 发布审计保留 `source.rust-only` 门禁，确保活动源码树不存在 C#、WPF、`.csproj` 或 `.sln` 遗留，正式构建与运行不依赖 .NET Runtime。
- 修复联网同步内存脚本的完成状态判定，确保同步结束后再检查主进程、短生命周期 PDF Worker 和临时文件残留。
- 完整继承 0.2.1 的托盘隐藏恢复全量重绘修复与纯 Rust 根目录结构。

## 0.2.1 — 2026-08-25

### 修复

- 修复 Slint 软件渲染窗口隐藏到托盘后，双击托盘恢复时偶发只重绘局部区域、其余客户区残留桌面背景的问题。
- 所有窗口恢复入口统一执行整窗背景失效，并在原生窗口完成显示后补一次全量重绘；覆盖托盘双击、托盘设置、通知点击、提醒弹窗和退出确认。
- 用户实机验证多次隐藏与托盘恢复正常；Rust 固定 fixture 与存储回归测试继续保持 16/16 通过。

### 仓库整理

- Rust 正式实现从历史 `prototypes/stock-ipo-reminder-rust` 路径迁移到仓库根目录的标准 Cargo 布局。
- 固定测试样本迁移到 `tests/fixtures`，应用图标迁移到 `assets`。
- 删除旧 C#/.NET/WPF 工程、测试、诊断/安装工具和本地 .NET SDK；正式源码、测试与发布链只保留 Rust。

## 0.2.0 — 2026-08-25

正式运行版本完全迁移到 Rust，不再依赖 .NET Runtime。该版本发布时旧 C#/.NET 源码仍作为历史基线保留，随后已在 0.2.1 仓库整理中移除。

### Rust 正式版

- 使用 Slint 提供任务列表、详情、设置、确认/撤销和人工字段覆盖界面。
- 使用 `windows-rs` 实现原生托盘、气泡通知、声音、单实例、Working Set trim 和登录计划任务。
- 迁移 Eastmoney、SSE、CNINFO、BSE 四来源同步、多来源裁决、来源健康和 1/2/4/8/15/30 分钟指数退避。
- 兼容旧版 SQLite schema，并迁移事件版本、提醒 Outbox、确认记录、人工覆盖、健康摘要、备份、维护和诊断包。
- 同一 Rust EXE 提供短生命周期 PDF Worker；采用 64 KiB 流式下载、32 MiB PDF/4 MiB HTML 上限、20 页、256,000 字符和 60 秒超时。
- 自动同步调整为启动时一次，之后每 24 小时一次；保留手动同步。同步完成后释放响应和正文缓存并执行 Working Set trim。
- 安装、升级、回滚、普通卸载保留数据、便携包和发布审计全部改用单个 Rust EXE。

### 稳定性与验收

- 修复新事件首次同步时公告早于事件落库导致的 SQLite 外键失败，并增加回归测试。
- 修复“确认已申购 → 撤销确认”后事件状态被重新写回已确认的问题；撤销、事件恢复和提醒重排现在处于同一 SQLite 事务。
- 同步错误不再被静默吞掉；失败会写入脱敏日志并恢复界面同步状态。
- 修复后台启动和窗口关闭生命周期：`--background` 不再短暂重显，点击右上角关闭后事件循环继续驻留托盘；只有界面“安全退出”、托盘退出或测试超时参数才结束程序。
- 应用 EXE、Slint 标题栏和系统托盘统一嵌入正式多尺寸图标；标题栏图标延迟到事件循环启动后安装，失败不会阻断程序启动。
- 固定 fixture 与存储回归测试 16/16 通过；真实联网同步完成 45 条近期候选、24 个事件、7 份公告，四来源成功、零来源失败。
- 最新正式 EXE 隔离同步中，主进程峰值为 13,701,120 bytes Private Bytes / 33,443,840 bytes Working Set；包含短生命周期 Worker 的进程族峰值为 25,030,656 / 60,645,376 bytes。
- 同步完成 5 秒后主进程为 13,701,120 bytes Private Bytes / 20,795,392 bytes Working Set；7 份 PDF 均已落盘，PDF Worker、Worker 请求/响应和下载临时文件残留均为 0。
- release smoke 空闲值为 5,095,424 bytes Private Bytes / 29,802,496 bytes Working Set；0.2.0 单 EXE 约 14.6 MiB，便携 ZIP 约 7.4 MiB，发布包不包含 `.dll`、`.deps.json` 或 `.runtimeconfig.json`。

### 已知限制

- 不连接券商、不自动下单，也无法验证券商是否受理委托。
- 发布物尚未进行 Authenticode 代码签名。
- Windows 11 Build 26200 已完成 smoke；Windows 10 2004+ 的最终支持声明仍需要对应系统证据。

## 0.1.1 — 2026-08-24

### 改进

- 更新应用、窗口标题栏、任务栏和系统托盘图标为新版“市场哨兵”设计。
- 图标包含 16–256 px 多尺寸资源，改善任务栏和高 DPI 环境下的辨识度。
- 托盘右键菜单显示当前版本号，并固定从底部任务栏上方弹出。

## 0.1.0 — 2026-08-24

首个 Windows 预发布候选版本。

### 功能

- 沪市、深市和北交所新股申购任务发现与多源核验。
- 常驻托盘、逐只人工二次确认、撤销确认和关键字段变化后重新确认。
- 每小时到每 2 分钟的分级提醒、午间恢复和安全截止处理。
- SQLite 持久化、提醒 Outbox、重启/休眠恢复、来源健康和每日摘要。
- 正式公告下载、哈希、字段证据、人工覆盖和待人工核验状态。
- 数据清理、SQLite 在线备份、日志轮转、脱敏诊断包和系统时间检查。

### 数据可靠性

- CNINFO 独立 Cookie 会话、预热、来源内节奏和健康空结果处理。
- SSE/CNINFO/BSE 公告正文与 `%PDF-` 文件签名校验。
- HTML/WAF 伪 PDF 在落盘前拒绝，并受控转入人工核验。
- 严格联网诊断覆盖四个主 Collector、预期公告 Provider、近期公告覆盖、PDF 签名和 SQLite 完整性。

### 发布

- 自包含 `win-x64` 便携 ZIP。
- 不需要管理员权限的每用户安装器。
- 升级前数据库一致性备份和程序目录原子替换。
- 普通卸载默认保留数据；删除数据必须显式二次确认。
- 发布清单和 SHA-256 校验文件。

### 已知限制

- 不连接券商、不自动下单，也无法验证券商是否受理委托。
- 首版发布物未进行代码签名，Windows 可能显示未知发布者提示。
- 当前 Windows 11 Build 26200 验证可用；Windows 10 2004+ 的最终支持声明仍需要对应系统 smoke 证据。
