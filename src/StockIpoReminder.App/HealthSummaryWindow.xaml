<Window x:Class="StockIpoReminder.App.HealthSummaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="打新提醒健康状态" Width="400" SizeToContent="Height"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="ToolWindow" ShowInTaskbar="False" ShowActivated="False"
        Style="{StaticResource AppWindowStyle}" Topmost="True">
    <Border x:Name="OuterBorder" Background="#111B2E" BorderBrush="#F59E0B" BorderThickness="2" CornerRadius="12" Padding="20">
        <StackPanel>
            <TextBlock x:Name="TitleText" FontSize="22" FontWeight="Bold" />
            <TextBlock x:Name="SummaryText" AutomationProperties.AutomationId="HealthSummary" Margin="0,12" TextWrapping="Wrap" FontSize="15" />
            <TextBlock x:Name="SourceText" Foreground="#94A3B8" TextWrapping="Wrap" />
            <Button Content="知道了" HorizontalAlignment="Right" Margin="0,16,0,0" Click="Close_Click" />
        </StackPanel>
    </Border>
</Window>
